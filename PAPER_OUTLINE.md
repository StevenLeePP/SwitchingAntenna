# 论文大纲 + 补充实验计划（TWC，约 15 页）

日期：2026-07-16
依据：R1–R22 冻结结果（[REVIEW_VERDICTS.md](REVIEW_VERDICTS.md)）、
[TECHNICAL_MANUAL.md](TECHNICAL_MANUAL.md)、场景评估（TX/TMA 不进本篇；
FAS 模式阶梯进本篇）。

---

## 第一部分：投稿前补充实验（预计 1–1.5 周）

> 原则：现有素材已超一篇 15 页 TWC 的容量，以下补充不是"凑内容"，而是
> 堵审稿人必问的三个洞 + 补全 FAS 定位。全部沿用预注册 + 成对 + TDL 优先纪律。

### E1【最高优先，堵最大的洞】在线（非 oracle）端口选择器

**动机**：R20 的选择器直接读 TDL 真值信道（`isOracleChannelSelector=true`）。
审稿人必问："估计信道下选择增益还剩多少？" 现在没有答案。
**做法**：扫描帧上用 DM-RS 估计 Ĥ（估计管线现成）→ 贪婪选择用 Ĥ 而非 H →
与 oracle 选择、M4 基线三方成对（20–50 seed，TDL，含损伤）。
**预注册判据**：估计版保留 oracle 增益的中位比例（预期缩水，如实报告）；
若增益消失 → 诚实边界"选择增益受信道估计质量门控"（与 Phase 2 结论呼应，
仍可发表）。
**成本**：2–3 天。**去向**：论文 §V 的一小节 + 一图。

### E2【FAS 定位必需】单用户 FAS 分集模式（模式阶梯第一级）

**动机**：论文以 FAS 语言定位，需要补上 FAS 文献的经典问题作为模式谱系起点。
**做法**：N=1，每相干期从 M 个 J0 相关端口选最强单端口（现有 J0 信道 + 端口
位置模型直接复用）；输出中断概率 / SNR 增益 vs M 与端口间距（0.125λ/0.25λ/0.5λ）。
与 Wong 的 FAS 分集结果定性对照；J0 非单调发现（R21）在此模式下是 FAS 社区
关心的结论。
**成本**：2–3 天，全复用现有代码。**去向**：§VII 模式阶梯 + 一图。

### E3【近零成本，把 100 ms 变成物理量】扫描周期 ↔ 相干时间/移动速度映射

**做法**：纯解析 + 一表。T_c ≈ 0.423/f_d，f_d = v/λ_c（λ_c=9.4 cm @3.2 GHz）→
把 R20/R22 的 updateSec 网格翻译成允许的移动速度。预计结论偏严格
（100 ms 更新 ≈ 准静态/游牧场景），**这恰恰把 §8.1 的适用图变得具体可信**。
**成本**：半天。**去向**：§VII 适用性一段 + 一表。

### E4【可选，篇幅富余才做】逐子带端口选择

612 子载波指标现成，按 RBG 分组选 S vs 全带宽单 S。FAS-Meets-OFDM 邻接贡献。
成本约 2 天。默认不做，进 future work。

### 明确不做（future work 一句话带过）

- TX 侧开关波束赋形（= 时间调制阵列领域，另一篇论文）；
- ISAC/DOA、RIS 结合（无硬件纯蹭热点）；
- 多 SNR OTA 重采（需衰减器/板卡时间；R15 诚实缩放已够用）；
- 真实开关 PCB（下一阶段硬件项目）。

---

## 第二部分：论文大纲（15 页双栏，含页预算与素材映射）

**建议题目**：
*Switched Single-RF-Chain Multi-User MIMO for 5G NR Uplink:
Impairment-Aware Reception, Physically-Constrained Port Selection,
and Energy-Efficiency Boundaries*

**一句话主线**：单宽带链 + 高速码相开关 = 受物理约束的时间-空间观测变换；
本文给出它的损伤边界、恢复算法、端口选择净收益与能效折中的完整可复现刻画。

### I. Introduction（1.5 页）

- 动机：RF 链/ADC 功耗随端口数增长；单链开关路线（引 GreenMO、FAS）；
- 核心矛盾：理想去交织容易，难在器件非理想 × 同步 × 系统闭环（手册 §1.2 四层）；
- **贡献列表 C1–C6**（手册 §1.3 直接改写）：
  C1 静态/动态损伤二分定理 + 实验验证；
  C2 捕获律 η·C(Q) 与 ICI-DF 及其迁移边界（flat 38–58% → TDL 3%，双门控归因）；
  C3 器件规格语言：PSD/相关时间而非单一 RMS、LO 拓扑条件排序、ε-budget；
  C4 物理约束端口选择：A[m,n,q]、F1 穷举/贪婪 92.4%、码叠加无增值（负结果）、
     AMC 解锁 +0.45–0.71 bit/s/Hz 净吞吐；
  C5 同步作为系统吞吐一部分：waterfall/假峰平台分离、开关代价归因（结构 0）；
  C6 统一能效账本（含扫描）：~69% 有效速率 × ~15% 功耗 → 4.39× 能效；
- 硬件边界声明（手册 §1.4，一段，直白）。

### II. Related Work（0.75 页）

GreenMO/单链码域（差异：5G-NR 波形、损伤建模、同步、码叠加负结果）；
FAS/可移动天线（差异：多用户复用 + 频选 + 同步联合）；TMA 一句话划界（TX 侧，
future work）；HBF 功耗模型；massive-MIMO 相噪（Björnson 2015，公共/独立 LO）。

### III. System Model（2 页）

- 单链标量流红线 y[n]、去交织、H̃=SᵀH、**Rn=σ²SᵀS**（防伪阵列增益）；
- A[m,n,q] 张量与约束表（码周期/占空比/覆盖/因果建立）——手册 §2.3/§3.1；
- 信道：TDL-A + J0 端口几何（间距绑定）；
- 损伤模型汇总表（用户级 4 项 + 开关级 3 项 + RX-LO），公式引
  IMPAIRMENT_MODELS，正文只留定义式；
- **二分定理**（陈述 + 一段证明梗概 + 指向实验验证表）。
- 素材：手册 §2–§4.1；图 1 = 架构图；表 1 = 损伤分类。

### IV. Impairment-Aware Reception（2.5 页）

- CFO 318 Hz 判决门限（理论 vs 实测 <4% 命中表）+ 逐用户补偿 288×；
- 静态泄漏 cond(L) 预测 23.8% vs OTA 25.2%（配对注入）；
- τc 结论：同 RMS 下 BER 差 10×（→ 规格须 PSD）；
- **捕获律 reduction=η·C(Q)**（三点消融图）+ ICI-DF flat 结果（Q/迭代/SNR 一图）
  + 器件包络 >1.25×；
- **迁移边界**：真值 capture 64% / 双门控 / TDL 3%——压缩成一小节 + 一张
  integration-boundary 图（R7–R14 五轮 saga 不展开，引 REVIEW_VERDICTS）；
- LO 拓扑小节：公共 vs 独立、相噪空间平均（引文献）、周跳修复、条件排序图。
- 素材：手册 §4.2–4.7、§6.3–6.7；图 2–5。

### V. Physically-Constrained Port Selection（2.5 页）

- F1/F2 可行集、门 1 穷举（+2.42 dB, p=2e-5）、门 2 贪婪 92.4%、
  **码叠加负结果**（架构简化论点）；
- 门 3 三态叙事图：**SINR +2.14 dB 正 → 固定 QPSK 负 → AMC 正**（招牌图）；
- M=8/12/16 净吞吐 +0.45/0.61/0.71 + 扫描周期边界（50 ms 负/100 ms 转正）；
- 理论：LMMSE 表征 + log-det 上界/鲁棒下界 + **选择增益≠阵列增益归因**
  （minChainArrayGain 为负）+ ε-budget + J0 非单调；
- **E1 新增小节**：估计信道下的在线选择器 vs oracle（增益保留率，如实）。
- 素材：手册 §6.10–6.13、R21；图 6–8 + E1 新图。

### VI. Synchronization Under Switching（1.25 页）

- P_acq 三机制：噪声 waterfall / 4 帧累积左移 5 dB（18/0, p=7.6e-6）/
  高 SNR 假峰平台 52%→70%；
- 开关获取代价归因：结构 0 pp、损伤 24 pp（75/76/52 三臂）；
- goodput 公式含 P_acq（不在成功帧上偷报 BER）。
- 素材：手册 §6.8、R13/R14；图 9。

### VII. Operating Modes and Applicability（1 页）

- **模式阶梯**：①单用户 FAS 分集（E2 新图）→ ②多用户 S=I → ③多用户 M>N 选择；
- **E3 映射表**：更新周期 ↔ 相干时间 ↔ 移动速度 → 适用图
  （低速/游牧/固定无线 = 甜区；高速 = 明确不适用）；
- 手册 §8 的适用/不适用场景压缩成半页 + 一表。

### VIII. Energy Efficiency（1.25 页）

- 统一组件口径表（GreenMO/MAX2829/AD9963 锚定，`isBillOfMaterials=false` 声明）；
- 主结果表：五架构功耗/理想能效 + **"~69% 有效速率 × ~15% 功耗 = 4.39×"**
  的诚实表述（不声称速率等同 DBF）；
- 扫描摊薄曲线（82.5→200.1）+ 能量正比（N=1..4: 0.721→1.925 W vs DBF 几乎不变）；
- full-stack AMC 锚点 65.2 单独报，不与基线理想值排名。
- 素材：手册 §4.10、§6.14；图 10–11。

### IX. Platform and OTA Anchor（1.25 页）

- 双路径平台 + 实时 C/MEX（~100 frame/s、NMSE −115 dB、dropNew=0）；
- 方法学一段：**预注册门禁、成对控制、双向逐位复现、22 轮审议**（这是审稿人
  给分点，明确写）；
- R15 OTA：15/15 方向一致、高 SNR 单簇、**模型偏乐观 2×（EVM² 门 no-go 如实报）**。
- 素材：手册 §5、§6.1、§6.9；图 12。

### X. Conclusion（0.5 页）

手册 §11 压缩：不是"无条件优于 DBF"，是一张可复现的适用图 + 器件-算法协同
设计语言；future work：真实开关 PCB、PRACH 标准化、在线选择器部署、TX/TMA。

**页预算合计 ≈ 14.5 页 + 参考文献。图 12–13 张（全部已有 PNG 或 E1–E3 产出）。**

### 必须砍掉的（留在仓库文档里，论文不展开）

- B 线 R7–R11 五轮诊断链 → §IV 一小节 + 引用；
- PSS vote 臂、hard/soft 消融、R15 采集细节 → 各一句话；
- Phase 1 六条 1-D 扫描 → 一张汇总表；
- 实时启动/队列工程细节 → §IX 一段。

### 预答审稿人（写作时对应埋好）

1. "选择器用真值信道" → E1 直接回应；
2. "和 GreenMO 区别" → §II 表 + 码叠加负结果 + 波形/损伤/同步三差异；
3. "帧结构非标准" → §III 声明脚手架 + PRACH future work；
4. "无真实开关硬件" → §I 边界声明 + §VIII 组件模型定性 + §IX OTA 锚点；
5. "增益条件太多" → §VII 适用图：条件明确本身就是贡献。
