# 开关非理想性注入与分析计划

> 按重要性/优先级降序排列。每个条目包含：物理含义、建模方法、预期影响、论文产出。
> 当前基线：理想开关 BER≈1e-8, EVM≈2%, totalMs≈9ms, dropNew=0。

---

## 优先级说明

| 标记 | 含义 |
|------|------|
| ★★★ | 核心贡献，必须有，论文承重梁 |
| ★★☆ | 重要补充，显著增加深度 |
| ★☆☆ | 锦上添花，按篇幅取舍 |

---

## 1. ★★★ 开关隔离度 / 通道间串扰

**物理含义**：RF 开关在"关"状态下并非完全断开，选中天线 q 时，其余 3 根天线的信号以衰减比 α 泄漏到 RF 链。

**建模方法**：
```
stitched(n) = S_q(n) + Σ_{k≠q} α_k · S_k(n)
```
- 参数：隔离度 IS = −20·log₁₀(α)，典型范围 −15 ~ −50 dB
- 注入位置：`direct_sample_raw` / `command_snapshot_virtual` 中，在 `raw(4m+q, q)` 选取后、int16→float 转换前/后，叠加 3 路泄漏
- 泄漏是**相干的**（带幅度+相位），不是简单加噪声

**预期影响**：
- H̃ = (I+E)H，E 矩阵的 (i,j) 元素 ∝ α（i≠j）
- 虚拟链间干扰抬升 → cond(H̃) 恶化 → BER 上升
- **产生论文第一条有意义的曲线**：BER vs 隔离度 (IS)，找拐点

**论文产出**：
- 闭式/半闭式 SINR 退化表达式（干扰为结构化、非白噪声）
- "支持 4 流 QPSK BER≤10⁻⁶，隔离度需 ≥ X dB" → 器件规格包络第一条

**实现难度**：低（纯软件，加 3 行代码，已是全速率 4 通道）

---

## 2. ★★★ 有限开关建立时间（跳变瞬态 / 样点间记忆）

**物理含义**：开关从一个天线切到另一个天线需要 tRISE + tSETTLE。在当前 122.88 MS/s 采样率下，每样点只有 **8.138 ns**，而 HMC197 级开关 tON≈10 ns。**这意味着在一个样点内切不完** —— 采到的值是"前两天线按建立比例的混合"。

**建模方法**（一阶 RC 建立模型）：
```
y[n] = (1−β)·y[n−1] + β·target_antenna[n]
β = 1 − exp(−Δt/τ),  τ ≈ tRISE / ln(9) ≈ tRISE / 2.2
```
- 参数 τ：建立时间常数，由 tRISE 导出
- 这是 **IIR 记忆效应**，不是瞬时串扰
- 注入位置：在 stitched 流上逐样点递推

**预期影响**：
- 相邻样点间的天线信号混合 → 虚拟链在时域被"涂抹"
- 频域：子载波间产生**结构化 ICI**（不同于热噪声的平坦 ICI）
- **这是你和 GreenMO 论文的关键区分点**：GreenMO 用 WARP 40 MS/s (25ns/样点)，开关 10ns，建立有余量；你在 5G 参数下（8.138ns），建立时间是第一道物理瓶颈

**论文产出**：
- BER/EVM vs tRISE 曲线
- **核心量化结论**："在 5G NR 30kHz SCS 下，122.88 MS/s 开关需 tRISE ≤ 3ns 才可忽略建立效应；当前商用 RF 开关（~10ns）会产生 X dB 额外 SINR 损失"
- 器件规格包络第二条
- 为"为什么需要更快开关 / 为什么可以降采样率"提供定量依据

**实现难度**：低-中（IIR 递推，逐样点，约 10 行代码）

---

## 3. ★★★ 多用户独立损伤（CFO / 定时偏移 / 相噪 / 功率不平衡）

**物理含义**：当前 4 "用户"是**一台 YunSDR 的 4 个 TX 口**（同 LO、同相噪、同功率、同定时）。真实上行多用户各自独立。

**建模方法**（在发射端模拟，注入到已有 reference 波形上）：

| 损伤 | 每用户参数范围 | 注入方式 |
|------|---------------|---------|
| 载波频偏 CFO | ±500 Hz (典型), ±2 kHz (极端) | 逐样点复旋 exp(j·2π·Δf·t) |
| 定时偏移 | ±36 samples (CP=72, 半 CP 裕量) | 对 IQ 波形做分数延迟（Farrow/Lagrange 插值） |
| 相位噪声 | −80~−100 dBc/Hz @10kHz offset | 单极/多极 PLL 相位噪声模型 |
| 功率不平衡 | ±6 dB | 缩放因子 |
| 大尺度衰落 | 0~20 dB 差异 | 缩放因子 |

**注入位置**：**离线仿真主干（第 1 步要做的事）** —— 在生成 4 用户波形后、开关扩频前

**预期影响**：
- CFO → 虚拟链上跨符号相位旋转 + ICI，DMRS 单符号估计跟不上
- 定时偏移 → 频域相位斜率（DMRS 可吸收 CP 内），**但开关相位对齐被破坏**（见第 7 条）
- 相噪 → CPE（公共相位误差），4 路独立相噪在合并后无法分离
- 功率不平衡 → H 矩阵列缩放恶化、RZF 噪声放大

**论文产出**：
- BER/EVM vs CFO/定时偏移/功率比 的单变量和多变量扫描
- 4 用户独立 vs 相干单 TX 的分离性能对比（**这是 "P2 多用户分离" 的核心结果**）
- 证明"单链 RZF 对用户间 CFO 差异的容忍度约为 ±300 Hz"

**实现难度**：中（需搭建离线仿真主干；但一旦搭好，所有扫描自动完成）

---

## 4. ★★★ 单链 LO 相噪（从 4 独立链 → 1 公共链）

**物理含义**：当前模型有 **4 路独立 RX**（每路有独立 LO），等于假设了 4 条独立下变频链。真实 GreenMO 只有 **1 个公共 LO** —— 相噪是**公共的**（所有虚拟链共享同一个相位抖动）。

**这是"偏乐观"还是"偏悲观"？**
- 独立 LO：每虚拟链有不同的随机相位 → 相噪等效于额外噪声，RZF 揉掉一部分
- 公共 LO：所有虚拟链同一相位旋转 → **CPE（公共相位误差）是相干的**，RZF 可能放大它

**建模方法**：
- 在 stitched 流上（合并后、ADC 前）加一个公共相位噪声过程
- 相噪模型：单极 PLL (3dB 带宽 f_c, 带外 −20dB/dec) 或 datasheet 导出
- 注入位置：`direct_make_grid` 的 CFO 去旋之前，对 stitched 流加公共相噪

**预期影响**：
- 公共相噪 → 跨 OFDM 符号的 CPE → DMRS（单符号）无法跟踪跨符号变化
- 与 4 独立 LO 对比，证明单链的 CPE 效应是独特损伤（不是简单的"更好"或"更差"）

**论文产出**：
- EVM vs LO 相噪水平 (dBc/Hz)
- 公共 LO vs 独立 LO 的对比曲线 → "单链架构的独特相位噪声特性"
- 为"单链需要什么级别的 LO"提供规格

**实现难度**：低（Matlab 相位噪声模型可直接复用）

---

## 5. ★★☆ 开关/采样时钟失配与抖动

**物理含义**：
- **失配（skew）**：开关时钟与 ADC 采样时钟有固定相位偏差 → 开关沿与采样沿不对齐
- **抖动（jitter）**：时钟边沿的随机时间偏移

**建模方法**：
- 固定 skew：采样时刻偏移 Δt → 捕获的值是"目标天线 + 泄漏的过渡值"
- 随机 jitter：ΔT[n] ~ N(0, σⱼ²)，每个样点独立或相关
- 注入位置：在 `direct_sample_raw` 中，不取 `raw(4m+q, q)` 而是取 `raw(4m+q+δ, q)` 或邻近样点的插值

**预期影响**：
- 固定 skew：特定虚拟链的采样点落在开关过渡区 → 该链 EVM 系统性偏高
- 随机 jitter：等效于采样时钟相噪 → SNR 退化，σⱼ=100ps → SNR 退化约 0.5-1 dB

**论文产出**：
- EVM vs skew/jitter 曲线
- "开关-采样时钟同步精度需 ≤ X ps RMS" → 器件规格包络

**实现难度**：中（需样点间插值 / 分数延迟）

---

## 6. ★★☆ 宽带 ADC 效应（量化噪声 + PAPR + 削顶）

**物理含义**：单链 ADC 需采样 **N×B 带宽**（122.88 MS/s），比每链独立 B（30.72 MS/s）的 ADC 噪声带宽大 4×。且 4 路扩频拼接后 PAPR 更高。

**建模方法**：
- 对 stitched 流施加 ADC 模型：量化 (ENOB 参数) + 限幅（clip level 参数）
- ENOB 典型值：8-14 bits
- PAPR 分析：stitched vs 单路信号的互补 CDF (CCDF)

**预期影响**：
- 低 ENOB → 量化噪声地板抬升
- 高 PAPR → 需要更大回退 → 有效 SNR 降低
- **与 4 独立 ADC 对比**：单宽带 ADC 是否因噪声折叠而需要更高的 ENOB？

**论文产出**：
- BER/EVM vs ADC ENOB
- "单链 N×B ADC 相比 N 个独立 B ADC，ENOB 需求多出 X bits" 
- CCDF 曲线展示 PAPR 代价

**实现难度**：低-中

---

## 7. ★★☆ 开关相位 ↔ 用户定时偏移的耦合（开关系统独有）

**物理含义**：见上一轮分析。虚拟链 q 的样点 = 开关相位 q·8.138ns 的采样。当某用户有非整数 raw 样点的定时偏移 τ 时，其信号能量落在**不同的开关相位组合**上 → **跨虚拟链泄漏模式改变**。

**这是普通 MIMO 接收机不存在、开关系统独有的耦合效应。**

**建模方法**：
- 给每个用户独立定时偏移（不限于整数）
- 在波形层面注入（分数延迟 FIR），然后进入开关扩频
- 观察 H̃ 矩阵随 τ 的变化

**预期影响**：
- 小 τ：H̃ 对角占优、但非对角元素增加
- 大 τ：H̃ 结构改变，某些虚拟链的能量泄漏到其他链
- 交互效应：定时偏移 × 隔离度 → 2D BER 热力图

**论文产出**：
- cond(H̃) vs 定时偏移 曲线
- "开关相位对齐是单链接收的独特同步要求"——P1 的理论小节

**实现难度**：中（需分数延迟 + 波形级仿真）

---

## 8. ★★☆ 占空比误差 / 谐波权重不均

**物理含义**：GreenMO 的理论假设精确 1/N 占空比 = 各谐波等权重等距折叠。实际占空比有误差 → 谐波权重不等 → 部分虚拟链增益偏大/偏小（类似各天线增益失配）。

**建模方法**：
- 占空比 = 1/N + ε（ε：占空比误差）
- 等效：虚拟链 q 的增益 g_q = sinc 项（由占空比决定）
- 或直接在 stitched 流上对不同相位的样点乘以不等权重

**预期影响**：
- 各虚拟链增益不等 → 等效 H 矩阵行缩放 → RZF 放大部分链路噪声
- DMRS 可部分吸收（static 部分），但无法吸收时变部分

**论文产出**：
- EVM 退化 vs 占空比误差
- "占空比精度需 ≤ X%" → 器件规格包络

**实现难度**：低

---

## 9. ★★☆ 插入损耗及各支路增益/相位失配

**物理含义**：
- 插入损耗：RF 开关有 ~1dB 插入损耗（HMC197 ~0.8dB），等效于每个虚拟链有固定衰减
- 各支路失配：不同天线路径的 PCB 走线长度差异、开关器件差异 → 支路间增益/相位失配

**建模方法**：
- 每根天线路径加固定复增益 g_k·exp(jφ_k)
- g_k：~−0.5 ~ −2 dB（相对变化）
- φ_k：±5° ~ ±20°

**预期影响**：
- 静态部分 → DMRS 吸收（无影响）
- 不同天线的频选响应 → 部分子载波的 H 估计偏差
- 温度/老化引起的时变 → DMRS 逐 slot 跟踪

**论文产出**：
- 支路失配可被 DMRS 吸收的边界条件分析
- "DMRS 周期（每 0.5ms）能跟踪 ≤X Hz 的时变失配"

**实现难度**：低

---

## 10. ★☆☆ 温度漂移与老化

**物理含义**：开关特性、放大器增益、LO 频率随温度/时间缓慢漂移。

**建模方法**：
- 参数缓慢时变：增益漂移 = 一次函数、LO 漂移 = 随机游走
- 在 60s 长时仿真中注入，观察 BER 的时间趋势

**论文产出**：
- "60s 内无需重新校准的条件"

**实现难度**：低（但需要与长时仿真配合）

---

## 11. ★☆☆ 开关非线性（谐波失真 / IP3）

**物理含义**：RF 开关的非线性会产生谐波和互调产物。HMC197 IP3 ~50dBm，在接收功率水平（−30~−60 dBm）下通常可忽略。

**建模方法**：
- 三次多项式非线性：y = x + α₃·x³
- 或 Rapp 模型（更适合开关）

**预期影响**：
- 大概率可忽略（接收功率远低于开关压缩点）
- 可作为一个"已验证可忽略"的点（也是贡献）

**论文产出**：
- "接收功率 ≤ −30dBm 时，开关非线性贡献 <0.1% EVM"

**实现难度**：低

---

## 12. ★☆☆ 宽带噪声折叠（来自带外干扰）

**物理含义**：开关扩频后信号占 N×B，N×B 之外的带外干扰/噪声经开关谐波折叠回带内。

**建模方法**：
- 在 stitched 流上叠加带外 CW 干扰
- 模拟开关的多谐波频率转换（每个谐波 = 一个折叠路径）

**预期影响**：
- 特定频率的带外干扰会折入特定子载波
- 程度取决于干扰功率和开关谐波抑制

**论文产出**：
- "单链接收机对带外干扰的脆弱性映射"——实用意义强

**实现难度**：中

---

## 实施路线图

```
Phase 1 (当前 → 2 周)
├── 搭建离线仿真主干（MATLAB，脱离 YunSDR 硬件）
├── 注入 #3 多用户独立损伤
├── 注入 #1 开关隔离度
├── 注入 #2 有限建立时间
└── 产出：第一组 BER/EVM vs 隔离度/tRISE/CFO 曲线

Phase 2 (2 → 5 周)
├── 注入 #4 单链 LO 相噪
├── 注入 #5 时钟抖动
├── 注入 #7 定时偏移 ↔ 开关相位的耦合分析
├── 注入 #8 占空比误差
├── 推导 #1 的闭式 SINR 退化表达式
└── 产出：理论章节框架 + 主力仿真结果集

Phase 3 (5 → 8 周)
├── 注入 #6 ADC 效应
├── 注入 #9 支路失配
├── 损伤感知接收机（利用 E 矩阵结构做白化）
├── 双变量扫描（隔离度×CFO, tRISE×定时偏移 等）
└── 产出：完整结果 + 器件规格包络表

Phase 4 (按篇幅取舍)
├── #10 温度漂移
├── #11 开关非线性
├── #12 宽带噪声折叠
└── 扩展 M>N + BABF
```

## 关键设计决策

1. **所有非理想性优先在离线 MATLAB 验证，不碰实时 C-MEX**。离线链路给出曲线后，选最重要的 1-2 项下放到 C 热路径做 OTA 验证。

2. **每项非理想性必须有独立参数**。开关隔离度 = 1 个 double、建立时间 = 1 个 double……而不是"加一点噪声看看"。每一项都是未来"器件规格包络"的一个轴。

3. **交互效应是论文加分项**。第 1+2 组合（隔离度×建立时间）、第 1+3 组合（隔离度×CFO）的 2D 扫描热力图比单项曲线说服力强一个量级。

4. **"已验证可忽略"也是贡献**。审稿人关心"你考虑了 X 吗"，被动回答不如主动展示"我们测试了 X，在 Y 条件下可忽略"。
