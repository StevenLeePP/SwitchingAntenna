# 从一条射频链到多用户 MIMO：本工程在做什么、损伤从哪里来、目前做到哪里

## 写给谁

本文面向理解基本数字通信和多天线通信、但不熟悉快速开关单射频链接收机的读者。
它不是运行手册，也不按研发阶段罗列脚本，而是回答四个问题：

1. 为什么一条射频链有可能接收多个同频用户？
2. 从天线到比特，中间实际发生了什么？
3. 系统中加入了哪些非理想性，它们分别造成了什么后果？
4. 现在已经证明了什么，尚未证明什么？

文中缩写会在第一次出现时解释，末尾另有术语表。

## 先用一句话概括

传统四天线基站通常给每根天线配一套射频接收通道。本工程研究另一种结构：让多根天线
按照不同的高速二值码接通或关断，把它们合成为一条更高速的模拟流；模数转换后再按码相
拆成若干条“虚拟射频链”，最后使用常规多输入多输出接收算法分离用户。

这里的核心不是凭空从一条观测中恢复四份信息，而是用四倍采样带宽换取四个互相错开的
时间观测。代价是开关、时钟和振荡器的非理想性会被一同带入系统。

目前最诚实的结论是：

- 四用户基础分离已经跑通；
- 固定不变的开关误差大多能被导频估计吸收；
- 随时间变化的开关误差会产生真正的误码，隔离场景下可以明显补偿；
- 频率选择性真实信道中，增强接收机的收益被压缩到约 3%，没有达到升级门槛；
- 多于用户数的天线端口选择平台已通过基础回归和第一轮 M=8 穷举门，但还没有加入
  开关损伤与扫描成本，不能称为净增益已经成立。

## 1. 为什么“单射频链”仍可能形成 MIMO 观测

### 1.1 传统接收机

MIMO 是“多输入多输出”（multiple-input multiple-output）：多个用户或发射层同时发送，
基站通过多个空间观测把它们分离。若有 4 根接收天线，传统全数字方案通常具有：

- 4 套低噪声放大、混频和滤波电路；
- 4 个模数转换器；
- 4 条同步采样的数字基带流。

这一整套从天线信号到数字采样的模拟前端称为一条射频链（RF chain）。全数字方案性能
灵活，但射频链和高速模数转换器的功耗会随天线数增加。

### 1.2 本工程的基本想法

令 `r_m[t]` 表示第 m 根天线在时刻 t 的模拟接收信号。高速开关给每根天线施加二值码
`c_m[t]∈{0,1}`，所有接通端口在唯一的模拟求和节点形成

\[
y[t]=\sum_m c_m[t]r_m[t].
\]

`y[t]` 是一条标量流，它才进入唯一的宽带接收链和模数转换器。若要形成 N 条虚拟链，
模数转换速率提高到单用户带宽的 N 倍；数字端按 N 个码相去交织，得到 N 组观测。

“虚拟射频链”不是额外的硬件射频链，而是从一条高速数字流中按时间码拆出的数字观测。
它与传统 N 条并行射频链在数学接口上相似，因此后端仍可使用常规 N×N MIMO 检测器。

### 1.3 现在的 4×4 实现

当前 N=4。单用户基带采样率为 30.72 MS/s（每秒 3072 万个复采样），开关侧采样率为
122.88 MS/s，即四倍速率。理想情况下，四个连续高速样点依次来自四根天线，数字端每
隔四点抽取一次，形成四条 30.72 MS/s 虚拟链。

当前固定映射相当于选择矩阵 `S=I`，即每条虚拟链只对应一根物理天线。后续 M>N 的
研究会允许一根天线在不同码相加入多条虚拟链，也允许一条虚拟链由多根天线相加。

### 1.4 必须说明的硬件边界

现有 YunSDR 板卡实际采集四路并行天线 IQ，再在数字域受控模拟高速开关。IQ 是复基带
同相/正交（in-phase/quadrature）采样。因而现阶段证明的是算法、同步和实时处理架构，
不是已经焊接完成的物理单射频链开关阵列。论文表述必须保留这一区别。

## 2. 一帧信号如何从发射端走到比特

### 2.1 reference：共同的已知基准

本项目中的 reference 不是泛指“参考信号”，而是一份保存好的完整发射基准包：它包含
10 ms 发射波形、帧结构、导频位置、QPSK 星座符号和每一层的发送比特真值。QPSK 是
正交相移键控，每个复符号携带 2 bit。

实时发射、离线仿真、C/MEX 接收机和 MATLAB 接收机都读取同一份 reference；MEX 是
MATLAB 可以调用的本地 C 编译模块。这样做的目的是保证不同实现比较的是同一批数据，
而不是两个看似相同但比特排列不同的波形。

### 2.2 获取：先找到帧从哪里开始

接收机刚启动时不知道一帧从哪个采样点开始，也不知道本振频率偏了多少。因此首先进行
acquisition，中文可称为“初始捕获”或“同步获取”。本项目使用 5G NR（New Radio，
5G 新空口）的同步结构：

- PSS：主同步信号，用于寻找候选帧头和小区标识的一部分；
- PBCH：物理广播信道，用于验证候选帧头是否真的能解码；
- SSB：同步信号块，是承载 PSS、SSS 和 PBCH 的整体结构；
- track：跟踪模式，捕获成功后只在预测位置附近做低成本搜索。

SSS 是辅同步信号（secondary synchronization signal）。需要强调：当前自定义上行
reference 把 SSB 放在帧首作为同步脚手架，这便于受控研究，但不是标准 NR 用户上行的
初始接入流程；后续需要迁移到标准 PRACH。

初始捕获失败不只是“没有峰”。更危险的情况是假峰：噪声或频率选择性信道在错误位置
形成更高相关峰。错误帧头会使后续导频和数据位置全部错位。

### 2.3 CFO：校正收发频率不一致

CFO 是载波频率偏移（carrier-frequency offset）。用户振荡器、基站振荡器和多普勒会使
实际载波频率与标称值不完全相同。公共 CFO 可以利用 PSS 或循环前缀估计；循环前缀
（cyclic prefix）是 OFDM 符号前复制的一小段尾部样本，用来抵抗多径并辅助同步。

多个独立用户可能具有不同 CFO。只估计一个公共值时，其余残差会使各用户星座随 OFDM
符号持续旋转。OFDM 是正交频分复用：把宽带信号拆为大量正交窄带子载波，并用快速
傅里叶变换（FFT）在时域和频域之间转换。

### 2.4 DM-RS：估计每个用户到每条虚拟链的信道

DM-RS 是解调参考信号（demodulation reference signal），即接收机事先知道的导频。
当前使用 NR Type-1 DM-RS。接收机在每个子载波上估计复信道矩阵
`Hhat`；帽号表示估计值，它不等于不可观测的真实信道 H。

DM-RS 的关键作用不仅是估计无线传播信道，还会把一部分固定开关增益、相位和链间混合
一并估计进去。这正是固定开关损伤通常不产生误码的原因。

### 2.5 RZF：从四条混合观测中分离四个用户

RZF 是正则化迫零（regularized zero forcing）。普通迫零对估计信道矩阵求逆，以消除
用户间干扰；当信道接近奇异时，求逆会强烈放大噪声。RZF 加入一个小正则项，在干扰
抑制与噪声放大之间折中。

信道矩阵的条件数 `cond(Hhat)` 衡量它离奇异有多近：越大说明用户空间方向越难分，
求逆越不稳定。这里报告的条件数是估计信道的条件数，包含导频插值和估计误差，不能
直接等同于真实物理信道的条件数。

### 2.6 判决和评价指标

均衡后把复符号映射到最近的 QPSK 星座点，称为硬判决。主要评价指标为：

- BER：比特误码率，错误 bit 数除以总 bit 数；
- EVM：误差矢量幅度，接收星座相对理想星座的均方根偏差；
- EVM²：EVM 的平方，对应归一化误差功率，独立误差在这一域中才能线性相加；
- outage：中断概率，例如同步失败导致该次信道实现根本无法进入数据解码。

有限数据中“没有观察到误码”不等于真实 BER 严格为零，因此正式结果还要给统计上界。

## 3. 系统到底加入了哪些非理想性

下面不按研发阶段，而按它们在物理链路中的位置来组织。

| 位置 | 非理想性 | 当前观察到的主要效果 |
|---|---|---|
| 用户侧 | 功率、定时、独立 CFO、相位噪声 | 固定功率/循环前缀内定时大多可吸收；独立 CFO 可造成 20% 以上 BER，但基础补偿有效 |
| 传播侧 | 白高斯噪声（AWGN）、TDL-A 多径、深衰和空间相关 | 使信道求逆困难，是平坦场景算法增益迁移失败的主要现实压力 |
| 开关侧 | 泄漏、固定建立时间 | 静态时主要改变等效信道、EVM 和条件数，不必然直接产生 BER |
| 开关时变 | 建立时间漂移、OU 相关快抖和边界位移 | DM-RS 无法完全跟踪，产生载波间干扰（ICI）；相关时间 1 µs 时 BER 可升到约 1% |
| 接收本振 | 公共/独立接收本振（RX-LO）、锁相环（PLL）相噪、公共相位误差（CPE）周跳 | 公共 LO 不一定优于独立 LO，排序取决于 PLL 带宽和信道条件数 |
| 同步 | 噪声、TDL 假峰、开关损伤 | 高 SNR 仍可能卡在假峰平台；多帧累积只能部分抬升成功率 |
| 实时实现 | 冷启动、队列积压、丢包 | 不一定改变单帧算法精度，但会破坏连续时间序列和实时 BER 有效性 |

### 3.1 用户和发射机侧

#### 用户功率不一致

不同用户到达基站的功率可能相差数 dB。模型直接缩放每个用户波形。只要动态范围和
信噪比仍充足，这种固定列缩放能被 DM-RS 估计。当前 ±3 dB 压力范围没有形成主导误码。

#### 用户定时不一致

各用户传播距离和启动时刻不同，会产生整数或分数采样延迟。模型使用带限分数延迟，
避免线性插值人为削弱高频。只要延迟仍落在循环前缀保护范围内，它主要表现为随子载波
变化的相位斜坡，可被逐子载波信道估计吸收。

#### 每用户 CFO

独立用户 CFO 是已验证的强损伤。压力场景中，未补偿四层 BER 为
`[0, 0.0137, 0.273, 0.259]`；利用相邻时隙 DM-RS 相位变化估计每用户残余 CFO 后，
降为 `[0, 5.87e-5, 9.49e-4, 0]`。

该基础估计器使用相隔 0.5 ms 的导频，因此无模糊范围只有 ±1 kHz。超过这一范围会发生
相位混叠：不同真实频偏产生相同观测相位。当前代码在接近边界时告警，但真正扩大范围
仍需要更短时间间隔或联合估计。

#### 发射机相位噪声

相位噪声是振荡器相位的随机波动。用户侧模型采用 Wiener 随机游走，即相位增量独立、
总相位方差随时间增长。它适合描述自由振荡或未被锁相环强约束的振荡器，不适合长期
描述已锁定的真实 PLL。

当前温和参数下，一个时隙内的相位漂移远小于 QPSK 的 45° 判决边界，因此不是 BER
主导项。这里的价值主要是建立参数到 dBc/Hz 相位噪声谱单位的正确映射；dBc/Hz 表示
相对载波功率、归一到 1 Hz 带宽的噪声功率密度。

### 3.2 无线传播信道

#### AWGN 和 SNR

AWGN 是加性白高斯噪声，表示热噪声等无法逐项建模的随机扰动。SNR 是信噪比，即有用
信号功率与噪声功率之比。仿真中 standard/补偿算法共享同一噪声 realization，才能进行
公平配对。这里 standard 指不加研究型增强的标准 RZF 基线；realization 指一次固定的
随机信道、噪声和损伤抽样；seed 是生成它的随机种子。

#### 平坦信道和 TDL-A

平坦信道假设所有已占用子载波经历近似相同的 MIMO 矩阵，适合代数回归和机制隔离，
但通常过于容易。

TDL-A 是 3GPP 定义的一类抽头延迟线（tapped delay line）模型。它用多个具有不同延迟
和功率的多径抽头产生频率选择性衰落：不同子载波可能经历完全不同的增益、相位和条件数。
当前实现还加入了空间相关，即相邻天线的信道不是独立抽样。

TDL-A 的重要作用是防止算法只在“漂亮的平坦信道”上获得虚假大增益。本工程最典型的
例子是 ICI-DF：隔离平坦场景有 38%--58% 相对 BER 改善，最终 10 个 TDL realization
上的改善只有 3.15%。

当前 TDL 实现是静态信道，多普勒时变尚未开启。多普勒是收发移动引起的信道随时间变化，
后续不能把它当作已经验证的功能。

#### 空间相关和深衰落

空间相关表示相邻天线接收到的信道相似。相关性越强，多根天线提供的独立空间信息越少。
频率选择性深衰落则使部分子载波的有效信号非常弱。两者会抬高信道条件数，并使判决错误
集中在少数难以恢复的子载波上。

面向更多端口的最新平台不再允许任意填写一个相关系数，而是根据天线位置和总孔径生成
相关矩阵。这样，增加端口数、增大孔径和改变相关性之间具有明确物理关系。

### 3.3 高速开关和模拟求和网络

#### 有限隔离度和关断泄漏

真实开关的“关闭”端口不会完全为零。隔离度用 dB 表示关闭端口相对接通端口的衰减；
隔离度越高，泄漏越小。模型把其他天线的复信号以给定幅相泄漏到当前码相。

固定泄漏看起来像链间干扰，但只要泄漏矩阵在一帧内不变，它会成为等效信道的一部分，
DM-RS 可以估计。实验中即使隔离度降到 15 dB，静态场景仍未观察到 BER 增长；代价主要
体现在条件数和 EVM，而不是直接误码。

#### 有限建立时间和开关瞬态

开关从一个状态切到另一个状态需要时间，输出不会瞬间到达新目标。这段过渡称为开关瞬态。
模型用一阶 IIR 响应描述：当前输出由上一状态和新目标加权得到。IIR 是无限脉冲响应滤波器，
表示过去状态以指数形式持续影响现在。

建立时间用 10% 到 90% 的上升时间标定。当建立时间固定时，这种跨码相混合仍是周期且
稳定的，通常可被 DM-RS 吸收。真正危险的是建立时间随温度、电源和驱动噪声变化。

#### 时变建立时间：慢漂和快抖动

模型把建立时间写成平均值、慢速漂移和快速随机抖动之和。慢漂对应温度等较慢过程；
快抖动对应带宽有限的驱动或电源噪声。

快抖动使用 OU 过程（Ornstein--Uhlenbeck）或等价离散 AR(1) 过程。AR(1) 表示当前值
等于上一值乘相关系数再加随机创新；OU 是其连续时间形式。相关时间越长，相邻样点越相似。

这一参数产生了重要结果：在抖动边缘方差相同的条件下，近似独立样点的 BER 约为
`1.7e-3`，相关时间 1 µs 时升到约 `8e-3--1.1e-2`。原因是快速独立扰动会在一个 FFT
窗口内部分平均，而慢相关扰动表现为 OFDM 符号尺度的信道漂移，单个 DM-RS 难以跟踪。

因此器件规格不能只写“抖动 RMS 小于多少”，还必须限制抖动功率谱或相关时间。RMS 是
均方根幅度；PSD 是功率谱密度，描述扰动功率分布在哪些频率。

#### 过渡时间抖动和采样边界位移

这两者容易混淆：

- 过渡时间抖动改变一阶开关响应的有效时间间隔，只有存在有限建立过程时才起作用；
- 采样边界位移直接把读取位置前后移动一个亚采样量，通过带限插值获得新样本，即使理想
  建立时间为零也仍然存在。

加入边界位移修复了旧模型的盲区：过去在建立时间为零时，所谓“抖动”实际上是空操作。
代码会记录超过半个高速样点后被截断的异常尾部，避免静默改变物理模型。

#### 占空比和码调度

占空比表示一根天线在一个 N 相码周期内被接通多少次。更多接通次数可能带来阵列增益，
也会提高切换次数、瞬态混合和功耗。当前 M>N 平台已经能验证占空比、链覆盖、相位归属
和非法同相扇出。Phase 3 进一步发现：允许一根端口进入两条码相的 Dmax=2 码叠加没有
稳定额外收益；简单的 Dmax=1 端口划分已经获得主要选择增益。这是架构简化结论，而不是
为了降低复杂度事先禁止码叠加。

### 3.4 接收机振荡器和同步

#### RX-LO 与 RX-PLL

LO 是本地振荡器（local oscillator），用于把射频信号混频到基带；RX-LO 特指接收机
本振。PLL 是锁相环（phase-locked loop），通过反馈限制振荡器相位漂移；RX-PLL 即
接收机本振的锁相环模型。

项目用有界 OU/单极 PLL 近似替代长期无界的 Wiener 模型。参数包括稳态相位均方根和
PLL 带宽：带宽决定相位变化得快还是慢。

研究比较了两种拓扑：

- 公共单 LO：所有虚拟链共享同一个相位过程，符合单物理接收链的结构；
- 独立 4-LO：每个物理接收支路有独立相位过程，作为传统多链对照。

最初假设公共 LO 总会更容易校正，但实验否定了这一点。慢 PLL 下，独立相位噪声可能在
多天线合并时发生空间平均；公共相位误差不能被这种平均消除。另一方面，在病态信道中，
独立相位会破坏不同接收行之间的相对相位，排序又可能反转。因此不存在一个适用于所有
信道的“公共 LO 规格可放宽多少倍”。

#### CPE 和周跳

CPE 是公共相位误差（common phase error），表示一个 OFDM 符号整体旋转。接收机可以
用判决结果估计一个公共角度再去旋。该方法只估计一个实参数，计算量很小。

当估计落到错误的 QPSK 象限时，会出现约 π/2 的相位周跳，整个符号大量误判。本工程
明确审计了周跳，而不是只报告平均 BER。公共 LO 加简单 CPE 能部分缓解问题，但没有
达到“与无相噪完全等价”的预注册目标。

### 3.5 实时计算系统

#### direct RX 和 MEX

direct RX 指无高频图形处理的实时接收模式。数据到达后由持久 C 消费者直接完成抽取、
CFO 去旋、FFT、DM-RS 信道估计、4×4 RZF、QPSK 判决和 BER/EVM 累积。

MEX 是 MATLAB 可调用的本地编译模块。本工程的 MEX 使用 C 实现计算密集路径，MATLAB
只负责低频 PSS/PBCH 捕获、时间/频率控制、健康检查和结果保存。Phase 2 的研究型
ICI-DF、DDCE 和 CPE 尚未移入实时 MEX。

DDCE 是判决引导信道估计（decision-directed channel estimation）：先硬判数据，再把
较可信的数据符号当成额外导频更新信道。它在隔离诊断中帮助定位了信道估计瓶颈，但没有
独立通过全栈升级门。

#### DMA、ring、pending 和 dropNew

DMA 是直接内存访问，板卡可以不经 MATLAB 逐样点搬运而把数据写入内存。ring 是环形
缓冲队列，用来解耦板卡生产速度和接收机消费速度。

- pending：尚未处理的 DMA block 数；当前每个 block 约 1 ms，因此 pending=10 约等于
  10 ms 待处理数据，而不是“10 个 OFDM 时隙”；
- highWater：运行期间 pending 的历史峰值；
- dropNew：队列满时被拒绝的新数据块数量。

两阶段启动先完成 PSS 冷启动，再丢弃 ring 中已经过期的历史数据，从新的对齐点启动持续
消费者。这样接收机不会用接近实时速率的解码器追赶几百毫秒旧数据。

## 4. 为什么固定损伤常常没误码，时变损伤却会出问题

这是整个项目最重要的物理认识。

如果开关泄漏、增益、相位和建立过程在一帧内保持不变，那么“物理天线到虚拟链”的映射
在每个子载波上等价为一个固定复矩阵 G。无线信道 H 与开关矩阵 G 合并为等效信道 GH。
DM-RS 直接估计 GH，RZF 无需知道 G 和 H 分别是什么，也能对合成结果进行均衡。

这种映射可称为多相 LTI 系统：LTI 是线性时不变，多相表示它按固定码周期重复。

一旦系数在 DM-RS 与数据之间变化，接收机估计的是旧矩阵，数据却经历新矩阵。变化还会
破坏 OFDM 子载波正交性，把一个子载波的能量扩散到相邻子载波，形成 ICI，即载波间干扰
（inter-carrier interference）。这部分不再能被一次静态信道估计吸收。

因此“隔离度低”或“建立时间长”本身不一定是 BER 主因；它们是否随时间变化，以及变化
发生在哪个时间尺度，往往更重要。

## 5. 为这些损伤设计了哪些补偿

### 5.1 逐用户 CFO 补偿

利用相邻时隙 DM-RS 的相位差估计每个用户的残余频偏，再让该用户的估计信道列随 OFDM
符号旋转。这一基础方法把最差两层约 26%--27% BER 压到 `9.49e-4` 和 0，证明多用户
独立振荡器并非不可分离。

### 5.2 ICI-DF：判决反馈消除载波间干扰

ICI-DF 是 ICI decision feedback，即“载波间干扰判决反馈”。基本流程是：

1. 先用标准 RZF 得到第一次符号判决；
2. 用已判符号重建本来应该接收到的信号；
3. 从残差中估计一个有限宽度的 ICI 频域卷积核；
4. 消除估计干扰后再次判决。

核宽 Q 表示每个子载波两侧纳入多少个邻居。hard 使用硬判符号，soft 使用带可信度的软
符号。genie 是仅用于诊断的“知道真值的理想助手”，例如直接知道真实开关状态；它给出
理论天花板，但不是可实现接收机。

隔离平坦场景中，Q=6/12/24 的 soft ICI-DF 相对 BER 改善约 36.9%/47.9%/53.2%；
Q=12 增加到三次迭代时约改善 57.9%。这说明时变开关 ICI 含有可恢复信息。

但在最终 TDL 多实现测试中，标准条件 BER 为 0.16567，三次 soft ICI-DF 为 0.16046，
只改善 3.15%，且仍有 1/10 信道 realization 反噬。原因不是 ICI 不存在，而是深衰落、
信道估计误差和模拟开关内部状态不可观测共同污染了核估计。因此它被保留为研究结论，
没有进入默认实时接收机。这里的“全栈”是指独立用户误差、TDL、多天线相关、开关损伤、
同步和实际信道估计同时开启，而不是只隔离一个理想化损伤。

### 5.3 acquisition 增强

单帧 PSS 合并在 8--26 dB 的 100-seed 测试中长期停在 52% 成功率，说明高 SNR 后的
瓶颈不是噪声，而是某些 TDL realization 产生远端假峰。对 4 帧相关能量做非相干累积后，
20/26 dB 成功率提高到 70%，但假峰平台没有完全消失。

在 20 dB 的配对归因中，switch-off、理想四相交织和含损伤开关的成功数分别为
75/76/52。理想交织几乎没有额外代价，约 23 个百分点的捕获损失来自联合开关损伤模型。

## 6. 离线结果和真实空口结果怎样对应

OTA 是空口（over the air）实验，即信号真实经过发射板、无线传播和接收板。离线实验则
从保存的 reference 出发，在 MATLAB 中生成信道、噪声和器件损伤。

为了避免拿两次不同采集做比较，OTA 配对验证对同一段 raw122 IQ 分别施加理想开关和
受损开关。raw122 表示四路 122.88 MS/s 复采样。只有 PSS、PBCH 和基准 EVM 均合格的
采集段才进入分析。

最终 15 段高 SNR 数据中，理想与受损路径都没有观察到 bit errors，且损伤引起的 EVM²
增量在 OTA 和离线预测中 15/15 同方向。但 OTA 中位增量为 0.83035，匹配 SNR 的离线
中位增量为 0.39315，比值为 2.112，超过预注册的 2 倍上限，因此正式判定为 no-go。

no-go 不是程序失败，而是“预先规定的验收条件未通过”。这里的正确结论是：在
33--43 dB 的单一高 SNR 簇中，两条路径的趋势和量级一致，但离线模型对新增误差功率
偏乐观约 2 倍。三档接收增益并没有产生真正分离的 SNR 点，因此不能声称已经完成多
SNR 定量标定。

## 7. 现在已经完成什么，还缺什么

### 已经站得住的部分

- 从已知 reference 到 BER/EVM 的完整离线链路；
- 四用户 PSS/PBCH 获取、DM-RS 信道估计、RZF 分离和 QPSK 判决；
- 独立用户 CFO 的基础估计和补偿；
- 静态与时变开关损伤的物理分类；
- 时变建立、OU 抖动、边界位移、相位噪声和 RX-PLL 模型；
- ICI-DF 在隔离场景中的恢复能力及在 TDL 全栈中的明确边界；
- acquisition 的噪声区、假峰平台和开关损伤归因；
- C/MEX 持久实时消费者和队列审计；
- 高 SNR OTA 功能与趋势锚点；
- M=8/12/16 受约束端口选择、完整损伤后的 AMC 净吞吐、理论上下界和统一能效模型。

### Phase 3 的完整结论

Phase 3 把 4 根天线扩展为 M=8/12/16 个候选端口，并用二值选择矩阵 S 和三维调度张量
A 描述端口如何加入虚拟链。BABF 是二值模拟波束成形（binarized analog beamforming）：
每个端口只有接通/关断，没有任意连续相位权。无论 M 多大，数字化前都必须先坍缩成
一列宽带标量流；代码会拒绝把一个物理端口在同一码相复制到多条并行链的非法扇出。

M=8 的 2520 个固定两端口调度穷举首先给出 `+2.420 dB` 中位最差用户 SINR 头room。
放开端口关停后，166824 个 F1 候选在 20/20 个 TDL seed 上不差于 M=4；贪婪算法保留
穷优增益的中位 92.4%。传统每链只选一个端口的 FAS 基线仅有 `+0.38 dB`，而受约束
配对选择达到 `+3.69 dB`，说明主要收益来自观察信道后的端口组合与条件数改善。

把 25 dB 隔离、20 ns 建立、OU 抖动、PSS 获取和扫描开销加入后，固定 QPSK 的 M=8
吞吐量曾经 no-go；原因是 QPSK 已接近饱和，约 2 dB 的 SINR 改善换不成更高码率，却要
支付扫描时间。冻结 AMC 链路抽象后，M=8/12/16 在 1 s 更新时的净吞吐分别增加
`0.45/0.61/0.71 bit/s/Hz`；更新周期 50 ms 时仍可能为负，100 ms 后才转正。因此正结果
有明确条件：需要自适应调制并把端口扫描摊薄到足够长的信道相干时间。

理论部分用标准 LMMSE SINR 给出二值 S 下的严谨表征和上下界，并逐位复现数值引擎。
它不是新容量定理。固定端口集的中位阵列增益甚至为负，进一步证明收益是选择分集而非
“多接端口自然相干增益”。J0 空间相关还表明更密的端口并不一定更好。

能效部分把 RFIC、ADC、开关、LO、基带和端口扫描放入同一组件模型。M=16、4 流、1 s
更新时，本文理想参考能效为 `200.1 Mbit/J`，DBF 为 `45.5 Mbit/J`，即标称约 4.39 倍；
但本文只获得约 69% 的 DBF 理想速率，且数字是器件资料驱动的模型，不是板卡功率计
实测。本文自己的完整 acquisition/损伤/AMC 锚点只有 `65.2 Mbit/J`，不能与缺少同等
全栈处理的 DBF/HBF 理想值直接排名。

### 必须保留的边界

- Dmax=2 码叠加在 flat/TDL 上都没有稳定增值，简单端口划分足够；
- 固定 QPSK 或不超过 50 ms 的快速扫描可能净亏损；
- acquisition-aware 选择读取真实 TDL 信道，是 oracle 上界，其同步改善不显著；
- M>8 使用贪婪算法，没有穷举最优差距标尺；
- 理论速率不含全波形残差，能效是统一组件模型而不是实物测量。

## 8. 后续工作

### 论文写作

R1--R22 实验已经关闭，下一阶段不是继续调参寻找更漂亮曲线，而是把结果组织成两条
相互支撑的论文主线：第一条是开关非理想性、损伤感知接收与器件规格；第二条是 M>N
端口选择、系统净增益、理论表征与能效。论文必须同时保留 fixed-QPSK no-go、Dmax=2
负结果、oracle 选择器和 OTA 高 SNR 单簇等限制。

SINR 是信号、干扰与噪声比。FAS 是流体天线系统（fluid antenna system），通过多个
空间端口的选择或切换获得信道优势；DBF 是全数字波束成形，每根天线具有独立射频链；
HBF 是混合波束成形，用模拟网络把多天线压到少量射频链。本工程不声称单链方案在速率
上战胜 DBF，而是量化以较低速率换取 RF/ADC 功耗降低的条件和代价。

### 器件和空口增强

- 使用外部衰减器或发射功率步进，把 OTA SNR 真正覆盖 5--25 dB，而不是仅改变接收增益；
- 制作真实高速 RF 开关板，结束“四路采集后数字模拟开关”的硬件边界；
- 开启 TDL 多普勒，研究移动信道与开关时变的共同作用；
- 把自定义同步脚手架逐步迁移到标准 PRACH。PRACH 是 5G 的物理随机接入信道，用于
  上行初始接入和定时获取；
- 若离线算法通过多信道门禁，再把必要部分下放到实时 MEX，而不是先为追求时延移植一个
  尚未证明有效的算法。

若未来得到新的实验授权，最有价值的增强是用 DM-RS 估计信道替换 oracle 选择器、制作
真实高速 RF 开关板并用功率计验证 R22 组件模型，以及用外部衰减器把 OTA SNR 覆盖到
5--25 dB。它们是后续增强，不影响 `phase3-freeze-2026-07-16` 的当前结论。

## 9. 常见误解

### “一条射频链”是否意味着一个低速标量就能恢复四用户？

不是。它使用一条四倍带宽的标量流，通过高速码相提供四组时间错开的观测。节省的是并行
射频链数量，不是凭空消除采样带宽和信息维度。

### 静态开关损伤没有 BER，是否意味着器件越差都没关系？

不是。静态损伤仍会恶化条件数和噪声放大；更严重时会使信道不可逆。并且真实器件参数会
随时间变化，动态部分无法被一次 DM-RS 完全吸收。

### ICI-DF 在平坦信道改善 58%，是否已经解决问题？

不是。最终 TDL 多实现只有约 3% 改善，未通过全栈升级门。58% 是机制存在的证据，3% 才是
当前真实信道条件下的可实现结果。

### OTA 两条路径都没有误码，是否证明模型完全正确？

不是。15 段数据处于很高的 SNR 区间，BER 不够敏感。EVM² 显示离线模型对损伤偏乐观约
2 倍，且没有覆盖目标算法工作的中低 SNR 区域。

## 10. 术语速查

| 术语 | 含义 |
|---|---|
| 3GPP | 制定 5G NR 等蜂窝通信规范的国际标准合作组织 |
| acquisition | 初始同步获取：从未知采样位置和频偏开始寻找可靠帧头 |
| AR(1) | 一阶自回归随机过程，当前值与上一值相关 |
| AWGN | 加性白高斯噪声，常用热噪声抽象 |
| BABF | 二值模拟波束成形，每个天线端口只有 0/1 权重 |
| BER | 比特误码率 |
| CFO | 载波频率偏移 |
| cond(Hhat) | 估计信道矩阵条件数，越大通常越难稳定求逆 |
| CP | 循环前缀，复制到 OFDM 符号前端的一段尾部样本 |
| CPE | 一个 OFDM 符号共同经历的相位旋转 |
| dB / dBc/Hz | 对数功率比 / 相对载波并归一到 1 Hz 的噪声功率密度 |
| DBF | 全数字波束成形，每根天线通常对应独立射频链 |
| DDCE | 判决引导信道估计，用已判数据辅助更新信道 |
| direct RX | C/MEX 持久消费者承担热路径的无高频绘图实时接收模式 |
| DM-RS | 解调参考信号，用于估计数据经历的信道 |
| DMA | 直接内存访问，板卡直接向内存搬运数据的机制 |
| EVM / EVM² | 星座误差的均方根幅度 / 归一化误差功率 |
| FAS | 流体天线系统，在多个空间端口中选择或切换 |
| FFT | 快速傅里叶变换，OFDM 调制解调的核心运算 |
| genie | 知道不可用真值的理想诊断器，只表示性能天花板 |
| HBF | 混合模拟/数字波束成形 |
| Hhat | 由导频得到的信道估计值 |
| ICI | 载波间干扰，能量从一个 OFDM 子载波泄漏到其他子载波 |
| ICI-DF | 使用第一次判决重建并消除 ICI 的判决反馈接收机 |
| IQ | 同相/正交复基带采样 |
| IIR | 无限脉冲响应滤波器，输出持续依赖历史状态 |
| LTI | 线性时不变系统 |
| MEX | MATLAB 可调用的 C/C++/Fortran 编译模块 |
| MIMO | 多输入多输出，利用多个空间观测分离并行数据流 |
| MS/s | 每秒百万个采样 |
| no-go | 预先冻结的验收门未通过，不等同于程序崩溃 |
| NR | New Radio，5G 新空口标准 |
| OFDM | 正交频分复用，把宽带信号拆成多个正交子载波 |
| OTA | 真实空口传播实验 |
| OU 过程 | 有均值回归和有限相关时间的连续随机过程 |
| PBCH | 5G NR 物理广播信道，用于验证同步和读取广播信息 |
| pending / dropNew | 缓冲区待处理块数 / 队列满时拒绝的新块数 |
| PLL / RX-PLL | 锁相环 / 接收机本振锁相环模型 |
| PRACH | 5G NR 物理随机接入信道 |
| PSS | 5G NR 主同步信号 |
| QPSK | 正交相移键控，每个符号携带 2 bit |
| raw122 | 四路 122.88 MS/s 原始复 IQ 数据 |
| reference | 保存的共同发射波形、资源映射、符号和比特真值包 |
| RF chain | 从天线到数字采样的射频接收硬件链路 |
| ring | 生产者和消费者之间的环形缓冲队列 |
| RMS / PSD | 均方根幅度 / 功率谱密度 |
| RZF | 正则化迫零 MIMO 检测器 |
| seed / realization | 随机种子 / 由该种子产生的一次具体随机实现 |
| SNR / SINR | 信噪比 / 信号与干扰加噪声之比 |
| SSB | 5G NR 同步信号块 |
| SSS | 5G NR 辅同步信号 |
| slot / layer | 时隙 / 一个并行用户或空间数据层 |
| TDL-A | 3GPP 抽头延迟线 A 型多径信道模型 |
| track | 捕获成功后在预测位置附近持续跟踪同步状态 |
| TX / RX / LO | 发射机 / 接收机 / 本地振荡器 |
| virtual RF chain | 从单一高速标量流按开关码相拆出的数字观测，不是额外硬件链 |
| Wiener 相位噪声 | 相位增量独立、累计方差随时间增长的随机游走模型 |

## 读者接下来可以看什么

- 需要运行系统：[`RUN_COMMANDS.md`](RUN_COMMANDS.md)
- 需要查看代码和实时架构：[`README.md`](README.md)
- 需要核对精确损伤公式：[`IMPAIRMENT_MODELS.md`](IMPAIRMENT_MODELS.md)
- 需要查看每次实验数据和裁决：[`EXPERT_REVIEW.md`](EXPERT_REVIEW.md) 与
  [`REVIEW_VERDICTS.md`](REVIEW_VERDICTS.md)
- 需要查看 M>N 端口选择的冻结计划：[`PHASE3_PLAN.md`](PHASE3_PLAN.md)
