# 单射频链 5G NR 上行 MIMO 接收机研究论文方向报告

> 本文档面向 TWC / TCOM 级别期刊。目标不是"复现 GreenMO + 加非理想性"，而是重新定义"快速射频开关作为可重构模拟前端"的理论与实践框架。

---

## 第一部分：现状诊断

### 1.1 "如果不用最客气的语言会怎么说"

我在 RUN_COMMANDS.md 看到的链路复现已成功。10s 采集 1042 帧、dropNew=0、BER 稳定在 0~1e-7 量级。这说明你的基础设施（YunSDR + MEX C 热路径 + 帧结构 + PSS 同步）**在工程上是扎实的**。

但作为一个**论文故事**，目前状态有几个硬伤：

**硬伤 1：现在的代码本质上是一台 4 路全速率相干的接收机，不是"单链"**

你的 4 路 RF 链同时**以 122.88 MS/s 全速率采集**，等于 4 套 ADC + 4 个 LO。你只是用数字处理捏了一个"开关"。在审稿人眼里，这等价于带了 4 条独立 RF 链的传统数字波束成形——没有功耗优势，没有射频前端的简化。BER=1e-8 是必然的，因为你没有引入任何开关特有的损伤。

→ 这是**最根本的 gap**。你和 GreenMO 的关键区别是，GreenMO 真正只有一条 RF 链（一个 LO、一个 ADC），而你目前是四条。**审稿人会直接问：你的架构到底省了什么？**

**硬伤 2："多用户"尚未真正实现**

你的 4 层共享同一个时钟、同一个 LO、同一个功率。这是四层相干 MIMO，不是四个独立用户。没有独立 CFO、独立相噪、独立功率控制，你无法验证你论文最重要的前提——"上行多用户的分离"。

**硬伤 3：创新点仅限于"非理想性分析"，缺乏算法贡献**

审稿人对纯仿真+参数扫描的容忍度很低。TWC/TCOM 期待的是：
1. 新问题定义 / 新模型 ✅（你正在朝这个方向走）
2. 新技术 / 新算法 ❌（目前没有）
3. 系统的性能评估 ❌（有待补全）
4. 足够多的实验证据 ❌（目前仅有单场景单组实验）

### 1.2 你的真正优势：三个没有人在意过的角度

虽然上面指出了不足，但你的**实验平台**可能是学术界唯一一个同时具备以下条件的：
1. 真实的 5G NR 帧结构（SSB、DM-RS Type-1、30 kHz SCS）
2. 可编程的射频开关逻辑（通过软件定义虚拟链的组合方式）
3. 全速率的四通道采集（意味着你可以有损注入任何开关非理想性，并与理想基线对比）
4. 完整的实时 C/MEX 热路径验证

换句话说：**你的实验室基础设施比 GreenMO 的 WARP 实验台适合做系统级研究**，只是目前缺少理论骨架和算法贡献。

---

## 第二部分：论文框架重构——"单链流体天线"

### 2.1 核心论点

这篇论文的核心论点应该是：

> 本文证明，快速射频开关可以抽象为一个**可编程的端口选择器**，其在码域等价于流体天线系统（FAS）。通过开关时序的调整，单射频链不仅能完成常规的空间复用/波束赋形，还能解决 FAS 领域一直面临的"端口数量受限"和"射频链成本"之间的根本矛盾。本文从理论、算法、实验三个层面系统化了这一范式，并给出了"怎样的开关可以落地"的器件规格包络。

**关键词**：Single-RF-Chain MIMO, Fast RF Switching, Fluid Antenna System (FAS), Binarized Analog Beamforming, OFDM, 5G NR, Port Selection, Device Specification Envelope

### 2.2 一个核心视角的转换：把"缺点"重新框架化为"优点"

| 你的当前状态 | 审稿人眼中的解读 | 你应该如何重新框架化 |
|-------------|----------------|-------------------|
| 4 路独立低噪放+ADC | "你浪费了 4 倍的 RF 硬件成本，不是单链" | "我们将全速率多通道作为理想分析锚点，并独立注入/分离开关损伤，实现了受控实验的完备性" |
| 只有 4 根天线 | "谈不上 massive" | "4 天线的分析和实验是 M>N 理论的基础验证，文中给出了到 M>>N 的扩展框架" |
| 没有硬件开关 | "你只是在做仿真" | "本文建立了一个完整的仿真-到-OTA 验证的回环，OTA 数据作为对仿真模型的现实锚点" |
| 预排列 4 个用户 | "没有独立损伤" | "本文采用分步注入的控制变量方法：先建立理想相干基线，再逐步注入独立损伤" |

### 2.3 将本文定位为 FAS 的一个子范式

FAS 领域的核心思想是：通过在天线物理口径上移动/选择辐射端口，获得空间分集和复用增益。

你的开关选择向量 S 的行来自天线 m 在 N 个虚拟流上的通断：S ∈ {0,1}^(M×N)

这意味着：**S 的每一列相当于 FAS 的一个"虚拟天线端口"**。M 根天线通过二值组合产生了 N 个"虚拟端口"。一旦你看清了这一点，你的论文就可以直接借用 FAS 领域的整套理论工具：
- 端口选择的最优化问题
- 端口数量与性能的取舍分析
- 空间相关性下的性能分析

但更重要地，你有一个独特优势是 FAS 领域没有的：**FAS 的端口选择通常只能在端口位置/编号的框架下进行；你的开关码设计给了你自由在频域控制虚拟链的相位响应。**

#### 你的论文与现有 FAS 论文的根本区别

| 属性 | 现有 FAS | 你的方案 |
|------|---------|---------|
| 端口定义 | 空间位置 | 开关码的相位响应 |
| 端口选择方式 | 机械/液晶移动 | 可编程开关时钟 |
| 切换速度 | ms 级 (液晶) ~ μs 级 (MEMS) | ns 级 (RF 开关) |
| 端口数量 | 硬件决定的物理端口数量 | 可编程决定，可在线调整 |
| 多用户支持 | 通常支持单用户或简单 FAMA | 支持完整的空分复用+波束赋形 |

---

## 第三部分：可消化的创新点矩阵（9 大创新点）

按照 **理论 / 算法 / 实验 / 设计** 四个维度组织。每个创新点标了"建议优先度"和"大致篇幅"。

### 维度 A：理论贡献（★★★★★ 最重要的，TWC 最认的）

#### A1. 非理想开关下等效信道的闭式 SINR 退化分析

**建议优先度**：★★★★★（必须做）

**核心思想**：将隔离度 ε、建立时间 τ_rise、时钟抖动 σ_j 导致的损伤建模为 E 矩阵，使得等效信道为：

```
H̃ = HS + E(ε, τ_rise, σ_j, ...)
```

其中 E 是**结构化**的（非白噪声），结构来自开关时序。推导 SINR 退化的闭式表达式：

```
SINR_k = |w_k^H H̃ f_k|² / (Σ_{j≠k} |w_k^H H̃ f_j|² + σ² + E_k(ε, τ_rise, ...))
```

**预期产出**：
- "在用户数 N=4, 目标 BER=10⁻³ 时，隔离度须 ≥ X dB、t_RISE ≤ Y ns"
- 交互效应的条件：联合约束 (ε, τ_rise) 的可行域边界，打包为一张"可行/不可行区域"图

**所需工作量**：约 3 周
- 矩阵推导：1 周
- MATLAB 验证推导与仿真的一致性：1 周
- 生成器件规格包络表：0.5 周
- 多参数扫描和绘图：0.5 周

---

#### A2. 开关系统下的同步理论极限（定时/CFO CRLB）

**建议优先度**：★★★☆☆（很有价值，可根据篇幅取舍）

**核心思想**：在开关+扩频+折叠后的接收信号上，定时/CFO 估计的 Cramér-Rao Lower Bound（CRLB）与普通接收机的 CRLB 有何不同？开关的"缺失 3/4 样本"的采样模式改变了 Fisher 信息矩阵的结构。

**关键问题**：
- 开关采样（周期性稀疏采样）是否改变了定时估计的理论精度？
- 多用户 PSS 叠加下的 CRLB 比单用户差多少 dB？

**预期产出**：
- "在开关采样模式下，定时 RMSE 的 CRLB 是 X，比传统 Nyquist 采样 + Y dB"
- 证明了开关系统定时估计的理论天花板

**参考文献**：Kay, S. M. (Fundamentals of Statistical Signal Processing, Vol. 1) 中的周期性稀疏采样 CRLB 分析框架

---

#### A3. 二值天线选择矩阵 S 的信息论容量分析

**建议优先度**：★★★★☆（推荐做）

**核心思想**：S ∈ {0,1}^(M×N) 的二值约束使得 H̃ = HS 的信道容量低于无约束 HS 的情形。推导二值约束下的可达速率。

```
C = max_{S∈{0,1}^(M×N)} log₂ det(I + ρ · (HS)(HS)^H)
```

- 问题特征：组合优化 + 容量表达式嵌套
- 部分结果：在低 SNR 下，最优 S 等价于选择 M 个最强天线端口（接近 FAS 的结论）
- 新结果：在高 SNR 下，需要 ML 解码或联合优化的 S 设计

**预期产出**：
- "二值约束下的容量损失不超过 X bps/Hz"
- "低 SNR 下的最优 S ≈ 最大化 H 的最小奇异值；高 SNR 下 ≈ 最大化容量"

**参考文献**：
- Molisch, A. F., et al., "MIMO systems with antenna selection," IEEE Microwave Magazine, 2004
- Wang, W., et al., "Optimal Sparse Beamforming with Antenna Selection," CCC 2025

---

### 维度 B：算法贡献（★★★★★ 必须有，否则不像 TWC）

#### B1. 损伤感知接收机——利用 E 矩阵结构做白化/均衡

**建议优先度**：★★★★★（必须做）

**核心思想**：既然 E 的结构可以被建模，那么接收机就不应该忽略它。设计一个"知道 E"的检测器：

```
方案 ①: 白化 RZF    —— 先对噪声+损伤协方差做 Cholesky 分解，然后标准 RZF
方案 ②: 结构化最大似然 —— 在 ML 检测中显式包含 E 的先验结构
方案 ③: 近似消息传递 —— 针对二值约束的迭代检测
```

**关键技术细节**：
- 将开关损伤建模为各子载波的**外加有色的干扰**（不同于热噪声，E 矩阵会导致干扰在子载波间有色）
- 白化滤波器的系数来自隔离度和建立时间的估计值
- 不需要额外的导频，仅在 RZF 前增加一次白化变换

**预期产出**：
- "在隔离度=−20dB 下，损伤感知 RZF 的 BER 比朴素 RZF 降低一个数量级"
- 提供与理论（A1 的 SINR 闭式）的交叉验证

**实现难度**：中（~ 2 周，纯 MATLAB 实现和验证）

---

#### B2. 基于开关的流体天线端口选择算法

**建议优先度**：★★★★★（核心卖点，必须做）

**核心思想**：将 S 矩阵的优化诉求公式化为一个**二值端口选择问题**，针对不同的约束条件提出求解算法：

```
问题 ①: SINR Max  —— max_{S} min_k SINR_k
问题 ②: Rate Max   —— max_{S} sum_k log₂(1+SINR_k)
问题 ③: Energy Fair —— min_{S} P_total  s.t. sum_k SINR_k ≥ γ
```

**可设计的三层递进算法**：

- **算法 1：贪婪递增选择**（复杂度 O(M·N)）—— 从空矩阵开始，每次选择使 min-SINR 提升最大的一根天线加入对应的虚拟链。复杂度低，可实时运行。
- **算法 2：松弛 + 量化**（复杂度 O(M·N²)）—— 将 0-1 松弛到 [0,1]，凸优化求解后量化回 0/1。提供性能上界。
- **算法 3：基于 FIM/CRLB 的感知辅助选择**—— 将同步的 CRLB（A2）作为选择准则的一部分，做到同步-通信联合优化。**这是最原创的贡献。**

**关键创新声明**：
- 与 FAS 的"端口选择"不同，你的端口（即虚拟链）不是物理上互斥的**—— 一根天线可以同时属于多组（通过叠加的开关码）**。这突破了 FAS 文献中"一个端口激活一个接收机"的约束。
- 因此，你解决的**天线-流分配**（Antenna-to-Stream Assignment）比 FAS 的端口选择更通用、维度更高。

**预期产出**：
- 算法复杂度分析表格
- "40 根天线 (M)、4 虚拟链 (N) 下，贪婪算法在 X 次迭代内达到最优性能的 Y%"
- 与全 DBF（上界）的容量差距 < Z%

**实现难度**：中（~ 3 周）
- 贪婪算法：1 周
- 松弛量化：1 周
- 感知辅助：1 周

---

#### B3. 逐子载波的自适应开关码设计

**建议优先度**：★★★☆☆（特色贡献，非核心）

**核心思想**：当前开关码对所有子载波是固定的（一个 S 矩阵作用于全带宽）。25-plet 频选信道下，不同子载波组的 H 矩阵差异很大。允许以子带为粒度改变 S。

```
在每个资源块组 (RBG) g 上:  S_g = arg max log₂ det(I + ρ · H_g S_g (H_g S_g)^H)
```

**额外复杂度**：S_g 的选择带来 O(N_rbg · M · N) 的复杂度损失。需要设计低开销的方法：
- DNN 预测 S_g（离线训练，在线预测）
- 相邻子带合并（低频选性）

**预期产出**：
- 频选信道下的吞吐 vs 子带粒度 曲线
- "4 子带+贪婪选择达到全子载波选择性能的 98%"
- 频选开关设计 → 实际的 5G NR 宽带系统增益

**参考文献**：
- Zhu et al., "Performance Analysis and Optimization for Movable Antenna Aided Wideband Communications," IEEE TWC, Dec 2024

---

### 维度 C：实验与评估（执行层面，决定了论文的说服力）

#### C1. 基线对比九宫格（必须的对比实验）

**你应该在 9 种组合下做对比**，这是审稿人期待的比较：

| 架构 | 用户间相参 (Coherent) | 用户间独立 (Non-coherent) |
|------|----------------------|-------------------------|
| 全数字 MIMO (上界) | 不现实基准（纯理论值） | **硬件理想锚点**（你的 4 独立链） |
| 单链理想开关（你的天花板） | 当前状态 | 移入独立损伤后 |
| 单链非理想开关（你的贡献） | +隔离度/建立时间 | **论文的核心结果** |

**每条路径必须给出**：BER、EVM、cond(H̃)、吞吐。

---

#### C2. 非理想性参数扫描（单变量/双变量）

这是 P3（器件规格包络）的执行面。建议扫描 4 组核心图：

**单变量**（6 条曲线）：
1. BER vs 隔离度 (−15 → −50 dB), 固定 τ_rise=理想
2. BER vs t_RISE (0 → 20 ns), 固定 IS=理想
3. BER vs CFO (0 → ±2 kHz), 固定 N=4, 理想开关
4. EVM vs 公共 LO 相噪 (−70 → −110 dBc/Hz @10kHz)
5. BER vs 时钟抖动 (0 → 200 ps_rms)
6. BER vs ADC ENOB (6 → 14 bits)

**双变量**（4 张热力图——**这是审稿人最喜欢看的**）：
1. BER = f(隔离度, t_RISE) —— **核心图，展示交互效应**
2. BER = f(CFO, 隔离度) —— **用户独立 + 开关损伤的耦合**
3. BER = f(功率不平衡, 隔离度)
4. cond(H̃) = f(定时偏移, 隔离度) —— **独有的开关耦合分析**

---

#### C3. 端口选择算法对比

对于 B2 提出的算法，需要对比：
1. 穷举最优解 (exhaustive, 仅 M≤8)
2. 贪婪递增（你提出的）
3. 松弛量化（你提出的）
4. 随机选择（baseline）
5. FAS 文献中的 Max-SINR 端口选择（baseline）

**指标**：可达速率、min-SINR、运行时间、复杂度（FLOPs）

---

#### C4. M>N 场景的仿真评估

将 M 逐步扩展到 8、12、16（通过多天线信道仿真，不在真实硬件上），展示：
- 波束赋形增益（M 增加 1 倍 → SINR 提升 ~3 dB？）
- 空间自由度（M 增加，相同 N 下的干扰抑制能力）
- 与 8-antenna DBF 的差距（你声称接近的 benchmark）
- BABF vs 全 DBF 的差距

> 这部分是 GreenMO 最令人印象深刻的结果。你需要复现这个**趋势**，但不一定需要达到同样的绝对值。

---

### 维度 D：系统设计与实践

#### D1. 链路自适应（AMC + 开关配置联合选择）

**建议优先度**：★★★☆☆

在运行时根据信道条件（估计的 SINR、CFO 环境）同时选择：
- MCS (QPSK → 16QAM)
- N (2 → 4 虚拟流)
- S 矩阵（天线组合模式）

**可实现自适应的维度**：
| 配置参数 | 可选值 | 自适应触发条件 |
|---------|-------|--------------|
| 虚拟链数 N | 2/3/4 | 总吞吐、用户数、信道条件数 |
| 调制方式 | QPSK/16QAM/64QAM | 估计的 SINR |
| S 矩阵 | 多种预定义模式 | 用户位置（DoA） |
| 帧持续时间 | 10m/20ms | PSS 跟踪稳定性 |

---

#### D2. 能效分析模型

这是对标 GreenMO 的必要章节。建立功耗模型：

```
P_total = P_switch_array + P_RF_chain(N) + P_ADC(N·B) + P_BB(N)
```
- P_switch_array = M × P_switch_per_switch（RF 开关功耗，约 1mW/switch @ 40MHz）
- P_RF_chain = f(N)（LO + 混频器 + 滤波器，当 N 变时共享）
- P_ADC = g(N·B)（ADC 功耗 ≈ 线性于采样率）
- P_BB = h(N)（基带处理 ≈ O(N³)）

对比架构：
| 架构 | M | RF 链数 | ADC 总采样率 | 总功耗 |
|------|---|---------|------------|-------|
| 全 DBF （上界） | 4 | 4 | 4×30.72 MS/s | P_DBF |
| 你的单链+开关 | 4 | 1 | 1×122.88 MS/s | P_single |
| GreenMO | 8 | 1 | 1×40 MS/s | P_greenmo |

---

## 第四部分：论文大纲建议

### 标题建议

**Option 1（保守/技术型）**：
> Single-RF-Chain Massive MIMO Receiver for 5G NR Systems: From Fast RF Switching to Fluid Antenna Port Selection

**Option 2（大胆/范式定义型）**：
> Reconfigurable Single-RF-Chain MIMO: Unifying Fast Antenna Switching and Fluid Antenna Systems under a 5G-NR-Compliant Framework

**Option 3（应用驱动型）**：
> When One RF Chain is Enough: 5G-NR Uplink Multi-User MIMO via Reconfigurable Antenna Switching — Theory, Algorithms, and Device Specifications

### 章节结构（建议的 30-40 页 TWC/TCOM 结构）

**I. Introduction**（约 5 页）
- 动机：Massive MIMO 能效瓶颈
- 现有方案及其局限（DBF、HBF、GreenMO、FAS/MA）
- 本文贡献清单（9 条核心贡献）
- 论文结构

**II. System Model**（约 4 页）
- 5G NR 上行帧结构和参数
- 开关扩频与时域去交织模型
- 虚拟链概念
- 信道模型和信号模型

**III. Non-Ideal Switch Effects Analysis**（约 6 页）← 理论核心
- 非理想开关的 E 矩阵建模（隔离度、建立时间、抖动、占空比）
- 等效信道 H̃ = HS + E
- SINR 退化的闭式表达式推导
- 同步 CRLB（定时和 CFO）
- 器件规格包络的推导方法

**IV. Proposed Algorithms**（约 7 页）← 算法核心
- 损伤感知接收机（白化 RZF + 结构化 ML）
- 基于二值开关矩阵的端口选择（贪婪 + 松弛量化 + 感知辅助）
- 自适应开关码设计（子带粒度 DNN/贪婪）
- 链路自适应与开关联合配置

**V. Simulation Results**（约 10 页）← 实验核心
- 仿真配置、基线定义、信道/用户参数
- 不同非理想性下的 BER/EVM/吞吐（6 条单变量 + 4 张双变量热力图）
- 损伤感知接收机增益（与朴素 RZF 对比）
- 端口选择算法对比（3 种算法 vs 2 种 baseline）
- M>N 扩展增益（M=4→8→12→16）
- 能效分析模型和对比表
- 器件规格包络综合表

**VI. OTA Experimental Validation**（约 4 页）
- YunSDR 实验台设置
- 两阶段启动、队列管理、实时性数据
- OTA 结果与仿真锚定（理想开关下的基线对齐）
- 注入组 vs 真实开关差异的讨论

**VII. Conclusion and Future Work**（约 2 页）
- 主要发现总结
- 对器件设计者的建议
- 未来方向：扩展到 PRACH 标准化 / 毫米波+开关 / 混合硬件原型

---

## 第五部分：推荐实验和测试矩阵

### 5.1 仿真参数表

| 参数 | 值 |
|------|-----|
| 子载波间隔 (SCS) | 30 kHz |
| FFT 大小 | 1024 |
| 活动子载波 | 612 (51 RB) |
| CP 长度 | 普通 CP (88/72 样点) |
| RX 采样率 | 122.88 MS/s (RF 前端输出) |
| 虚拟链采样率 | 30.72 MS/s |
| 调制 | QPSK / 16QAM |
| MIMO 检测 | RZF + 白化 RZF |
| DM-RS 配置 | Type-1, 单符号, l=2 |
| 信道模型 | TDL-A, TDL-B, TDL-C (3GPP) |
| 用户速度 | 0 / 3 / 30 km/h (表征时变信道) |
| 信噪比 | −5 ~ 25 dB |
| 每点 Monte-Carlo | 至少 1000 帧 |

### 5.2 建议的系列实验

**实验组 1：基线建立**（-> 确认你的基础设施与仿真主干一致）
- E1a: 理想开关 + 相干用户 → 验证 BER 与现有结果一致
- E1b: 理想开关 + 4 独立用户 → 多用户分离的基线

**实验组 2：非理想性扫描**
- E2a: BER vs 隔离度 (−15 ~ −50 dB)
- E2b: BER vs 建立时间 (0 ~ 20 ns)
- E2c: BER vs CFO (0 ~ ±2 kHz)
- E2d: BER vs 相噪 (−70 ~ −110 dBc/Hz)
- E2e: BER vs 时钟抖动 (0 ~ 200 ps_rms)
- E2f: BER vs ENOB (6 ~ 14 bits)
- E2g: EVM vs 占空比误差 (0 ~ 10%)
- E2h: cond(H̃) vs 定时偏移 (0 ~ 36 样点)
- E2i: BER vs 功率不平衡 (0 ~ ±12 dB)

**实验组 3：交互效应扫描**
- E3a: BER = f(隔离度, CFO) —— 双变量热力图
- E3b: BER = f(隔离度, 建立时间)
- E3c: BER = f(CFO, 定时偏移)
- E3d: cond(H̃) = f(隔离度, 定时偏移)

**实验组 4：算法对比**
- E4a: 朴素 RZF vs 白化 RZF（损伤感知）BER 对比
- E4b: 贪婪 vs 松弛量化 vs 穷举（M=8）的端口选择性能
- E4c: 自适应 MCS vs 固定 MCS 的吞吐

**实验组 5：M>N 扩展**
- E5a: M=4→8→12→16 下 SINR 累积分布
- E5b: M>N 的 BABF 下可达速率 vs 全 DBF

**实验组 6：OTA 验证**
- E6a: 理想开关基线（已有结果）
- E6b: 独立损伤注入（在现有 IRS 基础上加相位预编码）-> 验证仿真模型的预测
- E6c: 两阶段启动+窄窗 PSS 跟踪稳定性

---

## 第六部分：参考文献分类指南

### 6.1 核心引用论文（必须引）

**GreenMO 系列**：
- Gupta et al., "GreenMO: Enabling Virtualized, Sustainable Massive MIMO with a Single RF Chain," ACM MobiCom'23.
- Gupta et al., "PhaseMO: A Universal Massive MIMO Architecture for Sustainable NextG," 2025.

**FAS 基础理论**：
- K. K. Wong et al., "Fluid Antenna System — Part I: Performance Characterization," IEEE TWC, 2023.
- K. K. Wong et al., "Fluid Antenna System — Part II: Multiple Access," IEEE TWC, 2023.
- K. K. Wong et al., "A Tutorial on Fluid Antenna System for 6G Networks," IEEE COMST, 2025.
- New et al., "Fluid Antenna Systems: Redefining Reconfigurable Wireless Communications," IEEE JSAC, 2025.
- Hong et al., "A Contemporary Survey on Fluid Antenna Systems: Fundamentals and Networking Perspectives," IEEE TNSE, 2025.

**5G NR PHY**：3GPP TS 38.211-214 系列。

**天线选择 MIMO**：
- Molisch, A. F., "MIMO Systems with Antenna Selection," IEEE Microwave Magazine, 2004.
- Sanayei, S., Nosratinia, A., "Antenna Selection in MIMO Systems," IEEE Comm. Mag., 2004.
- Shen et al., "MARS: Message Passing for Antenna and RF Chain Selection," IEEE TCOM, 2024.

**硬件损伤的 MIMO**：
- Björnson, E. et al., "Hardware Impairments in MIMO," IEEE SPM, 2014（必引）.
- Björnson, E. et al., "Massive MIMO with Hardware Impairments," IEEE TWC, 2015.
- Zhao & Han, "DNN-based Two-stage Compensation for THz Hybrid Beamforming with Hardware Impairments," IEEE ICC, 2024.

### 6.2 文献定位地图

```
                            ┌──────────────────────┐
                            │     你的单链 5G NR     │
                            │  FAS 端口选择+非理想性  │
                            └──────────┬───────────┘
                                       │
            ┌──────────────────────────┼──────────────────────────┐
            ▼                          ▼                          ▼
    ┌─────────────────┐      ┌──────────────────┐      ┌───────────────────┐
    │     FAS 领域     │      │  单链 MIMO 转型    │      │ 硬件损伤 MIMO 领域  │
    │ Wong, Chae, Xu  │      │  ESPAR/LMA/RAA    │      │  Björnson, Han    │
    │  端口选择/容量    │      │  Han, Murch, Zhu  │      │  建模与补偿算法    │
    └─────────────────┘      └──────────────────┘      └───────────────────┘
                                       │
                              ┌────────┴────────┐
                              ▼                  ▼
                      ┌──────────────┐  ┌──────────────┐
                      │ 可移动天线 MA  │  │ 波束赋形 HBF  │
                      │ Zhu, Zhang   │  │ 天线选择    │
                      └──────────────┘  └──────────────┘
```

### 6.3 针对每个创新点的建议参考文献

| 创新点 | 引什么文献？ |
|-------|------------|
| A1 (闭式 SINR) | Björnson TWC 2015（硬件损伤闭式）、GreenMO（开关模型）、您的 FAS 系列建模 |
| A2 (同步 CRLB) | Kay, S. M. (统计信号处理)、FAS 的同步文献？(几乎没有 FAS 论文做过) |
| A3 (二值容量分析) | Molisch 2004（天线选择容量）、Wang 2025（稀疏波束赋形） |
| B1 (损伤感知接收) | Zhao & Han ICC 2024（硬件损伤补偿）、Björnson TWC 2015（补偿方法） |
| B2 (端口选择) | Wong TWC 2023（FAS 端口选择）、Shen TCOM 2024（消息传递天线选择） |
| B3 (子带开关码) | Zhu TWC 2024（宽带可移动天线）、Haider PLOS ONE 2025（动态天线聚类） |
| C1-C4 (实验) | GreenMO（基线设置）、所有对比架构的原始论文 |
| D1 (自适应) | FAS AMC 论文 |
| D2 (能效) | GreenMO（功耗模型）、FAS 能耗研究 |

---

## 第七部分：执行路线图

### Phase 0（已完成）：基础设施验证
- [x] 环境搭建（TX/RX 服务器，SSH 连接）
- [x] OTA 链路验证（10s 跑通，BER=0~1e-7，dropNew=0）
- [ ] **待办**：在本地搭建离线仿真主干（非硬件依赖的纯 MATLAB 链路）

### Phase 1（0-6 周）：基础链路 + 核心实验
- [ ] 离线仿真主干（从 reference 到 BER 的全链路）
- [ ] 多用户独立化（CFO、定时、相噪、功率）
- [ ] 核心非理想性注入（隔离度 + 建立时间）
- [ ] 第一组曲线（6 条单变量 + 4 张双变量热力图）

### Phase 2（4-10 周）：算法
- [ ] 损伤感知接收机（白化 RZF）
- [ ] 贪婪端口选择算法
- [ ] 松弛量化算法
- [ ] 算法对比（3+2 baselines）

### Phase 3（8-14 周）：理论
- [ ] E 矩阵建模和 SINR 闭式
- [ ] 同步 CRLB 推导
- [ ] 二值容量分析
- [ ] 器件规格包络表

### Phase 4（12-18 周）：扩展和写作
- [ ] M>N 仿真
- [ ] 能效分析
- [ ] 链路自适应
- [ ] 全篇论文写作、修订、内部审查

### Phase 5（16-24 周）：投稿准备
- [ ] 补充实验（审稿人预期但遗漏的）
- [ ] 代码和数据打包
- [ ] 格式审查、语言润色
- [ ] 投稿

---

## 第八部分：你要面临的审稿人问题（预计）

提前准备好这些问题可以省掉至少一轮 Major Revision：

1. **"你的架构到底省了什么"**
   → 回答：省了 N−1 条 RF 链，具体体现为 ADC+N 的噪声增加 vs N 个窄带 ADC 的功耗节省。需要在 D2 的能效分析中明确给出交叉点。

2. **"既然你的开关是数字模拟的，这和传统 DBF 有什么区别"**
   → 回答：关键区别在（a）损伤结构不同（共享 LO vs 独立 LO，定时偏移×开关相位耦合）；（b）M>N 时不同；（c）码域自由度是 FAS 的新维度。

3. **"为什么非要做 OTA 实验？你的仿真为什么不直接就够了"**
   → 回答：OTA 验证是"确认我们的仿真模型/假设在真实空口下成立"。你已有的 OTA 数据显示了"理想开关下仿真与 OTA 对齐"的锚点——这个锚点解决了很多审稿人对纯仿真论文的质疑。

4. **"你为什么不用真正的 RF 开关"**
   → 回答：（诚实版）因为没有时间和经费做定制 PCB。（战略版）"本文的目的是在非理想性注入受控、可重复、可独立解耦的条件下，系统评估开关参数与系统性能的关系。真实开关将多个非理想性（隔离度、建立时间、插入损耗等）绑定在一起，无法独立消融。我们的分步注入方法反而是更科学的。"

5. **"FAS 的端口选择和你的 S 矩阵选择有什么区别"**
   → 回答：区别在于（a）一根天线可以属于多个虚拟链（叠加码），突破了 FAS 的端口排他性；（b）开关码设计了虚拟链的相位响应，比 FAS 的空间位置选择多了频域控制维度。这是一个审稿人非常可能提出的问题，需要准备充分的对比分析。

6. **"你的单链能效比 4 链 DBF 好不好？"**
   → 回答：好（单 ADC + 单 LO 功耗 < 4 ADC + 4 LO）。需要给出数值估算。但 GreenMO 已经说过了。你的新东西是"在 4 流下，单链能效是 4 链 DBF 的 X 倍，同时容量达到 Y%"。需要精确计算。

---

## 第九部分：与可移动天线领域的交叉点

**这个领域的趋势和你的论文的关联**：

2024-2025 年 MA/FAS 领域最重要的技术进展：

1. **"FAS Meets OFDM"**（TCOM 2025）：FAS + OFDM + 5G NR 的首次融合。你的论文天然属于这个方向，且在"实际端口的选择算法"和"多流支持"上可以超过它。

2. **"6D Movable Antenna"**（Zhang's group, CUHK-SZ）：允许天线位置的 3D 移动 + 3D 旋转。你的开关不做"物理移动"——但你在**码域做选择**速度更快（ns vs ms）。这是一个本质优势。

3. **"Toward Practical FAS: Co-Optimizing Hardware and Software for Port Selection and Beamforming"**（TWC 2025）：提出的 GNN 端口选择算法 + FPGA 加速器。你的贪婪算法更简单、更适合在线实现。

4. **"Multi-Port Selection for FAMA: Massive Connectivity with Fewer RF Chains than Users"**（arXiv 2025）：这和你**最相关**。同样研究"比用户少的 RF 链"，但他们的解决方案是端口选择，你的方案是开关码分配。

**你能从 FAS 借用但需要本地化的东西**：
- 端口选择的空间相关性模型 → 本地化为你的天线布置模型的相关系数
- 中断概率的闭式分析 → 本地化为考虑开关码结构的闭式
- 中断容量 → 本地化为虚拟链的 SINR

**FAS 领域没有但你能做的**：
- 多用户带宽复用（BABF + 端口选择联合优化）
- 开关码频选自适应（FAS 论文没有频选优化）
- 同步 + 端口联合设计（FAS 不做同步）
- 64QAM/256QAM 下的开关损伤退化（FAS 大多只做 QPSK/16QAM 分析）

---

## 最终建议摘要

**你的论文最简洁的"卖点陈述"**（一旦有人问"你这篇论文究竟在做什么"）：

> 这篇论文从"快速射频开关 ≈ 可编程端口选择器"的观察出发，将单射频链 MIMO 重新定位为**流体天线系统的一个新子类**。
> 在这个框架下，我们（a）建立了非理想开关损伤的 E 矩阵理论，（b）设计了损伤感知的接收机和端口选择算法，
> （c）通过 5G NR 的 OTA 平台给出了从仿真到硬件的闭环验证，并（d）产出了"什么样的开关可以做"的定量器件规格包络。
> 我们的实验表明，在 5G NR 上行 4 用户空分复用场景下，即使使用当前商用 RF 开关（典型隔离度 −20 dB, t_RISE≈10 ns），
> 通过本文提出的白化 RZF 和贪婪端口选择算法，SINR 退化可以控制在 1.5 dB 以内，证明了单射频链 Massive MIMO 的落地可行性。

---

*本文档提到的所有文献可在 IEEE Xplore、ACM Digital Library、arXiv 获取。具体文献引用请根据最新检索确认页码和卷号。*

*文档生成时间：2026-07-13*
