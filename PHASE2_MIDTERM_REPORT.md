# Phase 2 中期总结报告（R11）

日期：2026-07-15（Asia/Shanghai）
性质：独立审议方（Claude）在完成 R11 全量复现后出具的中期总结 + 下一步执行细则。
复现日志：RX `/tmp/review_r11.log`；历轮裁决见 [REVIEW_VERDICTS.md](REVIEW_VERDICTS.md)。

---

## 0. 本轮验证声明（R11 复现结果）

以下全部经审议方独立验证，与项目方结论一致：

| 项目 | 项目方声称 | 审议方验证 |
|---|---|---|
| DDCE | BER/EVM 双 seed 改善，gap 仅 +1.6/+4.5pp，pass=false | MAT 审计一致（gain +0.0156/+0.0451，rankDef=0） |
| 约束核真值门 | capture 63.72%/64.52%，相对无约束 −0.13/+0.49pp，DOF 100→25 | MAT 一致 + **fresh 重跑逐位匹配** |
| Ĥx̂-代理物理核 | physical-only gap −56%/−64%，组合 −73%/−53%，不保留 | MAT 一致（−0.5615/−0.6364；−0.7301/−0.5262） |
| PSS 投票 | 5/8→6/8，救回 20261004，可选接口 | validate 通过（20261002 回退、20261004 双链救援） |
| 兼容回归 | R8 默认 BER 逐位不变 | 四模式理想链路 BER=0 通过 |

真值捕获从 R10 的 47.5%/51.3% 升到 63.6%/65.0% 的原因已定位并接受：R10 的
bank 对 {t[n], t[n−1]} 是近似；R11 换成精确一阶结构 {t[n], y₀_static[n−1]}，
天花板上移是回归量定义改进，非矛盾。

---

## 1. 我们现在在哪

Phase 2 的主线是一条五环链：**表征 → 机制 → 恢复 → 规格 → 集成边界**。
前四环已经闭合且全部经过双向逐位复现；第五环（全损伤栈上的集成增益）经
R7–R11 五轮迭代，以**三次预注册失败 + 真值天花板测量**收束成一个科学定论，
而不是一个悬而未决的工程目标。

## 2. 成果地图

| 环节 | 成果 | 证据轮次 |
|---|---|---|
| **表征** | 二分定理（静态损伤 polyphase LTI → DM-RS 吸收，−7.7 dB 混合下 BER 仍零）；BER 随 τc 单调升 10×（同方差）→ **器件规格应约束抖动 PSD 而非方差**；全部模型参数被独立理论命中（β 六位、floorHit 高斯尾、边界抖动 EVM 导数模型、截断计数泊松） | R2–R3 |
| **机制** | 捕获律 **reduction = η·C(Q)**，C(Q)=(2/π)arctan((Q+½)/(T_sym·f_c))，三点消融 ±4% 命中；genie 代数可逆性（信息未破坏） | R3–R4 |
| **恢复（隔离场景）** | ICI-DF：BER 降 38–58%；soft 优于 hard 8.5%；η(iter) 0.658→0.795；SNR 全程无反噬；ICI 地板压低 2.1× | R4–R6 |
| **规格** | 器件包络：soft-DF 放宽快抖容限**保证 >1.25× / 点估计 ≈1.3×**（区间安全表述），且放宽是 τc 依赖的 | R6 |
| **集成边界（本阶段新定论）** | 见下节 | R7–R11 |
| **方法学** | 预注册门禁五连全部按数据执行（含两次拒绝自己实现的功能）；真值重放（2.5e-16/0）；洁净孪生；双向逐位复现协议；11 轮审议记录 | 全程 |

## 3. 集成线（B 线）的定论与处置

### 定论（论文 Integration/Limitations 章的骨架）

在温和全栈场景（TDL-A ρ=0.3、TA 对齐 ±4 样点、fast=0.2/τc=1 µs、20 dB）上：

**真值层**（天花板，全部实测）：
- 核类模型对 ICI 的捕获天花板 ≈ **64%**（精确一阶 drive t−y₀）；
- 25 实自由度共轭对称 B(u) 与 100 自由度无约束双 bank 捕获相同（drop <0.5pp）——**物理约束无损压缩成立**；
- 分解四支：信道支主导（gap 47–53%）、判决支次要（2–5%）、jointGenie ≈ 50%。

**实践层**（三次预注册失败，各有定量归因）：
- 单 bank：gap 0.6%/3.5%（ICI 占残差 51–63% 但核未捕获）；
- DDCE：BER 改善但 gap 仅 +1.6/+4.5pp（<15pp 门），信道支的收益大部分不经由核路径兑现；
- Ĥx̂-代理物理核：gap **−56%/−64%**——真值 drive 需要模拟域 target/静态态，
  Ĥx̂ 代理缺静态 IIR 高频强调与泄漏合成结构，代理误差 ≫ ICI。

**一句话定论**：*全损伤栈上的 ICI-DF 增益受双重门控——信道估计质量 ×
模拟状态可观测性；隔离场景的强增益（38–58%）不能自动迁移，其迁移条件本身
是可量化的（真值天花板 64%、约束核无损、信道支 47–53%）。*
这是一个有数据支撑的结论章节，不是失败记录。

### 处置

B 线达到停止判据（三次预注册失败 + 失因定量归因 + 天花板已测），**冻结为定论**，
仅保留一次有界的最后尝试（下节任务 A）。无论其结果如何，B 线随后关闭。

## 4. R11 执行细则（本轮要做的）

### 任务 A：received-drive 物理核（最后一次有界尝试）

唯一未试且物理上可获得的 drive 构造——**单链 ADC 原始流本身**：

```
t̂[n] = (y[n] − (1−β₀)·y[n−1]) / β₀      （已知静态 β₀ 的 IIR 逆）
d̂[n] = t̂[n] − y[n−1] = (y[n] − y[n−1]) / β₀
```

y = stitched122 是接收机的真实观测（这正是单链架构的定义），不依赖 Ĥx̂，
也不依赖不可观测的 L0 静态态。已知风险：自参照污染（d̂ 含 δy 自身，量级
~ICI/信号 ≈ 1–2%）+ 20 dB 噪声（≈10% 回归量噪声）。

执行顺序（两道门，都过才算数）：
1. **真值门**：在 `type1_run_phase2_r11_constrained_model` 里把 drive 从
   t−y₀ 换成 d̂（received 版），跑同样的 capture 对比。
   **判据：capture ≥ 50%**（相对真值 drive 的 64% 掉 ≤14pp）。不过 → 直接冻结 B 线，任务 A 结束；
2. **接收机门**：过真值门才把 d̂ 接入 `crossfit_conjugate`（25-dof 已验证），
   判据沿用 combinationPassed（BER 双 seed 均降 + gap 相对 DDCE ≥+10pp）。

预算：一轮实现 + 双 seed 成对。**无论结果，B 线随即按第 3 节定论写入文档。**

### 任务 B：PSS 投票升默认的扩样

现有 6/8 vs 5/8 样本太小。扩到 **≥30 个信道 seed × 3 个 SNR 点**（如 14/20/26 dB），
vote 与 combined 成对成功计数 + 配对差异检验（简单 McNemar 即可）。
vote 全面不劣且在 ≥1 个条件显著优 → 升默认；否则保持可选接口。
这项同时是 R13 P_acq 曲线的地基（同一批采集数据两用）。

### 任务 C：文档收尾

把第 3 节"集成边界定论"作为独立小节写入 EXPERT_REVIEW.md（论文 Integration
章的直接素材），并在 IMPAIRMENT_MODELS.md 增补 Phase 2 公式（捕获律、
B(u) 共轭对称约束、精确一阶 drive 定义、真值天花板表）。

## 5. Phase 2 剩余任务与时间线

| 轮次 | 内容 | 估时 |
|---|---|---|
| R11 | 上节任务 A/B/C | 1 天 |
| R12 | **A3 公共 LO 相噪**：PLL 单极 PSD 替换 Wiener；公共单 LO vs 独立 4-LO 成对对比（单链架构的特征性损伤，GreenMO 未做过） | 1–2 天 |
| R13 | **P_acq(SNR) 正式曲线**：真值 timing 容差 + 假峰率定义，≥100 seeds × SNR 网格；vote/combined 对比并入 | 1 天 |
| R14 | **论文级最终曲线**：隔离场景 Q/iter/SNR/包络在 TDL 多信道实现上重跑 paper 档；集成边界图 | 1–2 天 |
| R15 | **OTA 多点交叉验证**（需板卡）+ 全文档定稿、git 提交整理 | 1 天 |

**Phase 2 预计还需 5–7 个工作日。** C 线（端口选择/BABF/M>N）已建议正式划为
Phase 3，不计入上表。

## 6. 论文资产就绪度

| 论文章节 | 素材状态 |
|---|---|
| System Model + 二分定理 | ✅ 齐（IMPAIRMENT_MODELS.md） |
| 隔离场景 DF 表征（η·C(Q)、SNR、迭代） | ✅ 齐，待 R14 换 TDL 平均 |
| 器件规格包络 | ✅ 齐（含区间安全表述） |
| 集成边界 | ✅ 定论已成，待任务 C 成文 |
| 同步（P_acq、PSS 分集/投票） | 🔶 R13 后齐 |
| 单链特征损伤（公共 LO） | ❌ R12 |
| OTA 锚点 | 🔶 单点趋势级已有，R15 补多点 |

## 7. 收官附录（R15，2026-07-15）

R12 已完成公共/独立 RX-LO 与 CPE/周跳审计，R13 已完成 100-seed acquisition
waterfall/平台/多帧累积，R14 已完成开关代价归因和 TDL paper-stop 最终曲线。R15
完成了 25/30/35 dB 三档、每档 5 段的板卡 raw122 采集和同参数配对注入；15/15 段
PSS/PBCH 合格且理想/损伤两支均为零误码，EVM 增量方向与 matched-SNR 离线预测
15/15 一致。

收官门目前存在待裁决边界：项目预注册的 EVM² 增量中位倍率为 2.112，略超 2×，
因此 `acceptancePassed=false`；专家指令字面的 RMS-EVM 增量倍率为 1.957，满足 2×。
为避免事后选择指标，两个结果同时冻结，Phase 2 暂不打 freeze tag。除这个语义裁决外，
Phase 2 代码与实验资产已齐备；Phase 3 端口选择/BABF 仍保持独立，不借此边界结果提前
启动。

| 收官资产 | 位置/入口 | 状态 |
|---|---|---|
| R12 RX-LO | `type1_run_phase2_rx_lo_statistics.m` | 完成 |
| R13 acquisition | `type1_run_phase2_pss_acquisition_statistics.m` | 完成 |
| R14 TDL/归因 | `type1_run_phase2_r14_*` | 完成 |
| R15 OTA capture | `type1_run_phase2_r15_capture_campaign.m` | 15 段完成 |
| R15 配对/matched-SNR | `type1_run_phase2_r15_ota_cross_validation.m` | EVM² 门 no-go |
| R15 双口径审计 | `type1_audit_phase2_r15_metrics.m` | 待审议语义裁决 |
