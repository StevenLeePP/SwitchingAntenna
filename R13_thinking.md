我先按 R13 补充做两个设计修正：
多帧累计必须在同一 TDL 实现上保持信道不变、每帧噪声独立；不能简单重复已有 noisy IQ，否则会把噪声也重复，虚构累计增益。我会构造“固定信号/信道/开关线性基 + 独立单位噪声基”，再按 SNR 缩放，保证 1/2/4 帧严格成对。
获取失败分类预注册为：正确 NID2 的真值 timing 邻域峰值相对背景中位数不足 6 dB记为 miss；超过 6 dB但最终选择错误记为 false peak。这样低 SNR waterfall 与高 SNR 远端假峰能分开，而不是把所有 argmax 失败混为一类。
论文的 phase-noise averaging 文献锚点需要精确引用，我会只核对原始论文/正式出版页，不用二手网页。




已搜索网页


上下文已自动压缩



我现在进入 R13 实施：先冻结统计设计并核对 R12 裁决原文，然后实现“固定信道、独立噪声”的 1/2/4 帧非相干累积获取器。这里全部是离线 MATLAB；远端只会用 sudo 启动 MATLAB，不涉及板卡、TX 或系统配置修改。




运行了多个命令


R13 的统计方案已经可以冻结：100 个固定 TDL realization、11 个 SNR 点、四个成对检测臂（单帧合并、单帧投票、2/4 帧累积），并在 20 dB 做同波形/同噪声的开关 on/off 对照。失败分类会固定为 falsePeak / miss / windowClip，不会跑完后再改阈值。




已读取文件运行了多个命令


正在做一个只读的 PSS 相关输出形状检查，以避免把 nrTimingEstimate 的内部峰值序列误当成可直接跨帧相加的统计量。这个检查不生成实验结论，只决定累积器的正确实现接口。




运行了多个命令


有一个关键实现约束已经核实：nrTimingEstimate 输出的是取绝对值后的相关幅度，不能把“信号相关”和“噪声相关”事后线性相加。我会在完全等价的复相关域先组合信号与噪声，再取模；这样同一 TDL、同一噪声 realization 能严格成对复用到全部 SNR，且不会伪造跨帧增益。




编辑了文件运行了多个命令


首个等价门没有通过，因此我按预注册协议停止了统计扫描。问题限定在复相关 helper 与 MATLAB nrTimingEstimate 的索引/参考波形一致性，不是 PSS 算法结果；我会先把这一处做到数值等价，再允许 R13 跑任何曲线。




编辑了文件运行了多个命令


为了避免 5 帧、122.88 MS/s 全量数组造成数 GB 峰值内存，我把实现定为流式四段：TDL 信道在 5 帧内保持同一 realization，每个 10 ms 获取段使用独立噪声和连续信号切片，开关前预热一个 slot 后再做相关。这样既不重复噪声，也不重置信道，同时每段仍保留完整的远端假峰搜索窗口。




编辑了文件运行了多个命令


两个门现在都达到机器精度：复相关相对误差 6.55e-16，固定轨迹下的开关线性拆分误差 2.27e-16。我现在只跑 4-seed smoke，先检查多帧累积、投票、失败分类和 20 dB 开关 on/off 的整条数据管线；通过后才会启动 100-seed 正式统计。




运行了多个命令


4-seed smoke 已跑通：在 −12 dB，单帧两臂均 0/4，四帧累积恢复到 2/4；在 −8 dB，2/4 帧累积均为 2/4；20 dB 的 on-switch 单帧为 2/4，而同 realization 的 switch-off 为 3/4。样本太少，暂不作物理结论，但说明 waterfall、累积救援与开关代价三条路径都能被观测。现在启动冻结的 100-seed 正式运行。




运行了多个命令


正式运行前 5 个 seed 没有出现门禁或数值异常；每个 seed 都在写 checkpoint。当前已出现“switch-off 成功、on 失败”的配对样本，说明 20 dB on/off 对照具备辨识力，但会等 100 个 seed 和 McNemar 结果后再判断是否显著。




进度 11/100。20 dB 上四个 on-switch 臂目前几乎总是同成同败，这提示高 SNR 平台更可能由 realization/峰结构而非观测帧数决定；但低 SNR 上累积臂的作用仍需最终曲线分开判断，不能从这段日志提前否定。




进度 16/100，已经出现一个 20 dB 的 accum4 独立救援（前三臂失败、4 帧成功）。这说明高 SNR 平台并非绝对不可改善，但是否达到预注册显著性仍取决于净 rescue/loss 和精确 McNemar，当前不会修改任何检测参数。




进度 22/100。20 dB 又出现两例 2/4 帧累积救援；其中一例 switch-off 也失败，说明“累积修复远端峰选择”和“移除开关损伤”不是同一个作用机制。最终报告会把这两种干预严格分表，不混成同一收益。




进度 28/100，checkpoint 正常更新，没有 MATLAB 异常或失去远程会话。当前观察到的 4 帧救援并非只发生在低 SNR，正式结果将重点核对它在 14/20/26 dB 是否形成净收益，以及失败是否从 falsePeak 转成成功。




进度 34/100。日志仍显示单帧投票与单帧合并高度一致，而 4 帧累积能救回一部分 realization；这与 R11b“30 seeds 对投票检验力不足”的判断一致，也解释了为什么 R13 必须把多帧累积作为独立臂。结论仍等待完整配对统计。




进度 40/100。20 dB 已出现投票、2 帧和 4 帧三种救援类型；程序继续保持冻结阈值和相同噪声基底。正式结果中会同时报 rescue 和 loss，避免只挑“救回来”的样本造成偏差。




进度 46/100。出现一例 on 成功、off 失败，所以“移除开关一定改善 PSS”也不能先验成立：开关后的四相虚拟观测会改变支路权重和峰竞争。20 dB 的代价最终必须用双向不一致数和 McNemar 表述，不能只报成功率差。




正式运行过半（50/100），checkpoint 完整。已经看到 accum2 成功而 accum4 失败的样本，说明更多帧并非逐 realization 单调改善——额外帧也可能积累错误峰能量。最终会分别报告 2 帧和 4 帧，而不会把它们合并成“累积必然更好”。




进度 56/100，运行稳定。到目前为止，高 SNR 失败中既有所有臂都失败的 realization，也有投票/累积可救的 realization；这正是 R13 要分解的“实现平台”内部结构，后面会用 falsePeak/miss/windowClip 计数判断主导项。




进度 62/100。正式运行没有触发任何相关等价、开关线性或窗口完整性断言；目前每个观测段都包含完整 PSS 参考长度，所以若最终 windowClip=0，这会是采集设计保证的结果，而不是漏记。




进度 68/100。当前高 SNR 的 on/off 差异主要是 off 对部分 on 失败的救援，但也已有反向样本；多帧累积则出现净救援迹象。完整跑完后会先做精确统计核对，再写文档，不会依据实时日志直接下结论。




进度 74/100，预计还需约 5–6 分钟。到现在 acquisition-only 路径保持稳定，未调用 PBCH 或完整解码；这能确保最终 P_acq 曲线的计算成本和失败归因没有被 BER 接收链混入。




进度 80/100。20 dB 的日志已经显示 switch-off 不能解决所有 realization 平台失败，而 2/4 帧累积也不能救全部失败；最终结论会明确区分“噪声不足”“开关造成的获取代价”和“TDL realization 的远端峰竞争”三类边界。




进度 87/100，剩余 13 个 realization。统计脚本在每个 seed 后保存原始 success/timing/NID2/PMR/failure code，因此最终报告中的任何计数都可以从 MAT 逐项追溯，而不是只保存汇总曲线。




进度 94/100。最后阶段仍未出现错误；跑完后我会先独立重算 Wilson、McNemar 与失败分类汇总，核对脚本保存值，再更新 EXPERT_REVIEW.md 和复现命令，避免把绘图层或维度 reshape 错误带进结论。




100-seed 正式统计完成。头条结果已经清楚：单帧合并在 8–26 dB 固定 52%，确认高 SNR realization 平台；4 帧累积在 20/26 dB 提到 70%，而 20 dB switch-off 为 75%，on/off 的不一致数是 24/1、p=1.55e-6。我正在做独立汇总核验，尤其检查 8 dB PMR 失败分类在极低 SNR 下是否受“真值邻域取最大值”的多重比较偏置影响。




运行了多个命令


已查看 2 张图像



独立计数审计已通过。高 SNR（14/20/26 dB）所有失败都归为 falsePeak，miss=0、windowClip=0；低 SNR 的单帧合并则由 miss 主导并逐步转为假峰竞争。需要保留一个限定：投票臂用“四链 PMR 第二大值”判可见性，在纯噪声区会受次序统计量抬高，因此低 SNR 下投票的 falsePeak/miss 细分只作诊断，不作为论文主结论。




编辑了文件运行了多个命令


代码、文档已同步到 RX 服务器，7 个文件的本地/远端 SHA-256 全部一致，git diff --check 通过。现在做最后的 acquisition 等价回归和既有 PSS vote 接口回归；不会重跑 100-seed 曲线，也不会改默认实时接收路径。