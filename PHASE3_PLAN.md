# Phase 3 规划 + 全项目进度复盘

日期：2026-07-15；冻结状态更新：2026-07-16
配套文档：[RESEARCH_REPORT.md](RESEARCH_REPORT.md)（最初调研）、
[PHASE2_MIDTERM_REPORT.md](PHASE2_MIDTERM_REPORT.md)、[REVIEW_VERDICTS.md](REVIEW_VERDICTS.md)（R1--R22 审议）

> **冻结说明**：下文保留的是 Phase 3 启动时的预注册计划，不再代表待办。R16--R22
> 已依次完成平台、穷举标尺、贪婪/松弛、全栈净增益、理论与能效审议，最终冻结于
> `phase3-freeze-2026-07-16`。核心正结果是 AMC + 至少 100 ms 扫描摊薄时，M=8/12/16
> 净吞吐增加 `0.45/0.61/0.71 bit/s/Hz`；统一理想速率口径下 M=16 相对 DBF 能效为
> `4.39×`。边界是 Dmax=2 码叠加无稳定增值、固定 QPSK/过快扫描 no-go、同步感知选择
> 仍是 oracle 上界、M>8 无穷举标尺、能效不是板卡实测。下一阶段为论文写作。

---

## 1. 最初的目标（一句话）

发一篇 TWC/TCOM 级期刊：**5G-NR 上行、基站只有一条射频链、用快速射频开关把多个
同频用户分开**；核心卖点是把"开关"重构成"可编程流体天线端口选择器"，并给出
"什么样的开关一旦做出来就能落地"的器件规格。四根支柱：P1 同步、P2 用户分离、
P3 非理想性→器件规格包络、P4 端口选择/BABF/M>N。

---

## 2. 已完成 vs 最初目标

| 支柱 | 最初设想 | 现状 | 证据 |
|---|---|---|---|
| **P1 同步** | 开关+扩频后还能不能 PSS 找帧头 | ✅ **完成** | P_acq(SNR) 100-seed、多帧累积（−8dB 14→54）、开关同步代价归因(23pp 全来自损伤、交织结构 0) — R13/R14 |
| **P2 用户分离** | 单链能否 MIMO 分开 4 用户 | 🔶 **部分**：**基础分离已完成**（DM-RS/RZF + 逐用户 CFO 基础补偿有效）；**面向时变开关损伤的增强接收机**在真实 TDL 全栈只取得 ~3% 增益，未达升级门槛（no-go 是"增强没兑现"，不是"分不开"） | R7–R11、R14 |
| **P3 器件规格包络** | 非理想性→性能→"要什么开关" | ✅ **完成，且是全项目最强的一块** | 二分定理、捕获律 η·C(Q)、τc-PSD 结论、ICI-DF 恢复、规格包络 1.25×、LO 拓扑 — R1–R6、R12 |
| **P4 端口选择/BABF/M>N** | 单链流体天线、波束成形、massive MIMO | ❌ **完全未开始 = Phase 3** | — |

### 一句诚实的话

Phase 1–2 实际建成的,是**"开关非理想性 → 损伤感知接收 → 器件规格"这半篇论文**
(P1 + P3 + 良性 P2),它已经完整、且是 TWC 级严谨度。而**最初当作头号卖点的
"流体天线 / 端口选择 / M>N massive MIMO"(P4),一行还没写**——那正是 Phase 3。

也就是说:当前工作是 **M=N=4、S=单位阵**这一档(每条虚拟链 = 一根天线),
证明了"单链开关接收机在非理想性下能工作、代价可量化"。而"用比用户多的天线
做波束成形/端口选择"这个让它配得上 massive-MIMO / 流体天线叙事的部分,是 Phase 3。

---

## 3. Phase 3 详细安排

主题:**从 S=单位阵扩展到 M>N 二值 S 矩阵,把开关做成流体天线端口选择器。**
关键纪律(Phase 2 血的教训):**从第一天就在 TDL 真实信道上测,不要只在平坦信道
刷大增益**——ICI-DF 的 38–58% 就是这样被 TDL 压到 3% 的,端口选择增益极可能同样
被压缩,必须提前用真实信道验证,否则又是一个"平坦漂亮、全栈崩盘"。

### Part A：平台扩展到 M>N（地基，约 1 周）

- 开关模型从 `S=eye(4)` 推广到二值 `S ∈ {0,1}^(M×N)`,M=8/12/16;
- 实现"一根天线经码叠加同时属于多条虚拟链"(GreenMO 的核心机制,当前没有);
- S 的物理约束建模:占空比守恒(每列 1 的个数受码周期限制)、因果性 — 防止把
  物理开关夸张成任意二值矩阵(EXPERT_REVIEW 早已埋的问题);
- 信道扩到 M 天线 + Kronecker 空间相关(复用已验证的 TDL-A 基础设施);
- 代数回归:M=N 时退化回现有单位阵结果、逐位一致(沿用每轮的验证纪律)。

### Part B：端口选择算法（核心贡献,约 1.5 周）

三层递进 + 基线,全部在 TDL 上评估:

1. **贪婪 max-min-SINR**:从空 S 开始,每次加一根使最差用户 SINR 提升最大的天线。
   复杂度 O(M·N),可在线;
2. **松弛 + 量化**:0-1 松弛到 [0,1] 凸求解后量化,给性能上界;
3. **感知辅助选择**:把 P1 的同步 CRLB/信道质量并入选择准则,做同步-通信联合优化
   ——这是最原创的一条(FAS 文献不做同步);
4. 基线:随机 S、FAS 文献的 Max-SINR 端口选择、全数字 DBF(上界)。

指标:可达速率、min-SINR、复杂度、运行时间,vs M(8/12/16)、vs 空间相关性。
**要回答的关键问题:M>N 的波束成形增益在 TDL 下还剩多少**(GreenMO 的头条趋势)。

### Part C：流体天线定位 + 能效（叙事与对标,约 0.5 周）

- 把架构正式定位为**单射频链流体天线系统(single-RF-chain FAS)**——最新文献
  (FAS Meets OFDM, TCOM 2025;单链 FAS 端口选择)都在这条线上,你有差异化:
  多用户带宽复用 + 频选自适应 + 同步联合,是 FAS 文献没做的;
- **能效模型**:P = M·P_switch + P_1RFchain + P_ADC(N·B) + P_BB(N),对标 GreenMO /
  DBF / HBF,给出"4 流下单链能效是 4 链 DBF 的几倍、容量达几成"——这是对标
  GreenMO 的必需章节。

### Part D：理论补全（与 B/C 并行）

- 二值约束 S 下的闭式/半闭式可达速率(现在只有 cond(L) 特征值分析和捕获律,
  没有完整 SINR 闭式);
- 可选:开关采样下的同步 CRLB(周期稀疏采样如何改 Fisher 信息)——纯理论加分项。

---

## 4. 论文策略:建议拆成两篇,而不是赌一篇巨稿

Phase 2 的教训是**平坦增益会被真实信道压缩**,Phase 3 有同样风险(端口选择在 TDL
下可能也只剩个位数增益)。把所有筹码压在一篇依赖 Phase 3 成功的巨稿上,风险高。
建议:

- **论文 1(现在就能写)**:《单射频链开关天线 5G-NR 上行接收机:非理想性表征、
  判决反馈恢复与器件规格包络》。= P1 + P3 + 良性 P2 + 集成边界。**已完整、TWC 级、
  不依赖 Phase 3**。先把这篇银行入账。
- **论文 2(Phase 3)**:《单射频链流体天线 massive MIMO:二值端口选择与波束成形》。
  = P4 全部。这是原本想当头条的流体天线故事,独立成篇。

好处:先落一篇稳的,Phase 3 即使增益被 TDL 压缩,也是论文 2 的诚实结论,不会拖垮
论文 1。若坚持一篇大稿,则 Phase 3 必须成功且增益可观,否则整篇被拖累。

**这个拆分是你的决定;我推荐两篇。**

---

## 5. 更远的愿景（Phase 3 之后）

按"离落地还有多远"排序:

1. **迁移到标准 5G**:自设计帧(SSB 放上行做同步脚手架)→ 标准 PRACH 上行同步。
   这是"一旦器件做出来就能插进 5G"承诺的最后一公里,也是把非标准帧的审稿风险
   彻底消除;
2. **真实射频开关硬件**:定制 PCB(HMC197 级开关)。这是把定位从 TWC/TCOM 期刊
   抬到 MobiCom/NSDI 系统顶会的唯一杠杆——目前纯仿真+单平台,天花板就是好期刊;
3. **热点延伸**(按篇幅/精力):宽带/毫米波 + 开关、ISAC(感知通信一体)、
   与 RIS 结合——都是 2024–2025 FAS/MA 文献的活跃方向,可作为论文 2 的 future work
   或论文 3 的种子。

---

## 6. 现在的推荐动作

1. **收尾 Phase 2**:按 R15 裁决把 OTA 缩放表述定稿、打 `phase2-freeze-2026-07-15`、
   分主题提交 git;
2. **并行启动论文 1 写作**:素材已齐(IMPAIRMENT_MODELS + 各轮 MAT/图 + 审议记录),
   这是最高性价比动作;
3. **Phase 3 用与 Phase 2 相同的预注册+逐位复现+TDL-优先纪律推进**,产出论文 2。

> 注:项目方决定先跑完 Phase 3 再决定删减/合并,倾向融合到一篇。§7 的门 3
> (净增益边界)使这个选择安全:即使增益被压缩,P4 仍可作为"损伤感知端口选择
> 折中与边界"一节并入同一篇,不依赖"打赢 DBF"的强声称。

---

## 7. Phase 3 执行规范（信号模型、约束、算法、门禁）

> 本节是 Phase 3 开跑前冻结的执行规范。所有代码、实验、声称以本节为准；
> 任何偏离须在 REVIEW_VERDICTS.md 显式记录并说明理由。

### 7.0 三条不可违反的边界

1. **单链是一条标量流**：整个架构必须坍缩成 `y[t] = Σ_m c_m[t]·r_m[t]`，
   每采样一个标量。代码中任何时刻并行读 M 根天线 = 作弊。当前 4 路 OTA 采集
   是"数字模拟"，论文只能称 **"数字仿真 + OTA 原始 IQ 驱动的单链开关架构验证"**，
   不得声称物理单 RF 链 FAS 原型。
2. **码域复用 ≠ 模拟并接扇出**：一根天线进多条虚拟链，靠它在多个码相位都开
   （码叠加），不是同一时刻扇出到多路。用三维张量 A 表达以杜绝写错。
3. **P4 的框架**：从第一天框成"单链 + 码周期仅 N + 真实开关约束下的端口选择
   折中与边界"，不是"我们做出了 massive MIMO / 打赢了 DBF"。

### 7.1 信号模型（可直接实现）

记 M 物理天线、N=4 用户=虚拟链数=过采样倍数、`H ∈ C^{M×N}`（`H[m,u]`=用户 u 到
天线 m 的信道，逐子载波）。

- **单链标量流**：天线 m 由周期 N 的二值通断码 `c_m[·]` 驱动，
  `y[t] = Σ_m c_m[t]·r_m[t]`，`r_m = Σ_u H[m,u]·x_u + 噪声`；
- **去交织得虚拟链**：`v_c = Σ_m S[m,c]·r_m`，`S[m,c] = c_m[p(c)] ∈ {0,1}`；
- **等效信道**：`v = H̃·x`，`H̃ = Sᵀ·H ∈ C^{N×N}`，RZF 求逆分离用户。
  `M=N`、`S=I` 时 `H̃=H`，退化回现有四相系统（代数回归第一条）。

### 7.2 三维调度张量 A 与约束集

`A[m,n,q] ∈ {0,1}`：m 物理天线、n 虚拟链、q 码相位。干净映射
`A[m,n,q] = S[m,n]·1[q=n]`；保留 q 以严格表达硬件约束：

| 约束 | 表达式 | 物理含义 |
|---|---|---|
| 码周期 | `q ∈ {0,…,N−1}` | 过采样=N，相位数不可超 N |
| 占空比 | `d_m = Σ_n S[m,n] ≤ D_max` | 天线进几条链；↑则波束成形强但损伤/功耗↑ |
| 链覆盖 | `Σ_m S[m,n] ≥ 1, ∀n` | 每条虚拟链至少一根天线，否则死链 |
| 因果/建立 | 相邻相位可在建立时间内切换 | **把端口选择耦合到 Phase 2 建立时间模型** |
| 单链可实现 | `y[t]` 为标量和，无并行扇出 | 见 7.0.1 |

`S[m,n] = Σ_q A[m,n,q]`（或 OR）。**S 管信道数学，A 管物理约束。**

### 7.3 端口选择优化（Part B 核心）

变量 = A（等价带 A 级约束的 S）。目标（贪婪 max-min-SINR）：

```
max_{S 可行}  min_n  SINR_n(H̃, E)
```

**关键的相反耦合**：`SINR_n` 同时依赖
- `H̃ = SᵀH`：一链加同相天线 → 波束成形增益↑、条件数改善；
- `E(S)`：损伤作用在 `y=Σ_m c_m r_m`，一相位内天线越多 → 求和泄漏 + 跨相建立
  混合越重 → `E↑`。

⇒ min-SINR 随"每链天线数"**先升后降**。该峰是 P3(损伤)×P4(选择)的缝合点，
**FAS/GreenMO 均不联合建模选择的损伤代价——这是本文差异点**。

三算法（全部同 seed/信道/噪声成对比，平坦 + TDL 双跑）：
1. 贪婪 max-min-SINR：空 S 起，每次加使 min-SINR 提升最大且满足占空比/因果的
   (m,n)；在线低复杂度主算法；
2. 松弛 + 量化：0-1 松弛凸求解后量化，给上界与复杂度-性能折中；
3. 感知辅助联合选择：把 P1 同步 CRLB/信道质量并入准则，同步-通信联合，最原创。

基线：随机 S、FAS Max-SINR、固定均匀端口、全 DBF(上界)、合理 HBF。

### 7.4 净增益判据（go/no-go 门 3 的定量化）

```
NetGain(M) = BeamformingGain(M) − ImpairmentCost(M) − ScanOverhead(M)
```

- 某 M 使 `NetGain(M) > 0` → 正结果；
- 全程 `< 0` → **诚实的边界结论**（"此开关约束下码域端口选择不划算"），
  同样可发表，不得包装成正算法。

### 7.5 三个 go/no-go 门（预注册）

1. **架构门**：M=8 穷举最优必须证明受约束 A/S 相对 M=N=4 有增益；
2. **算法门**：贪婪在多 TDL realization 上稳定保留穷举增益的大部分（非仅平坦）；
3. **净增益门**：加入建立/隔离/acquisition 后 `NetGain(M)` 在某 M 为正，否则记为
   边界结论。

### 7.6 交付图

1. min-SINR / 速率 vs M(4/8/12/16)，六条线(贪婪/松弛/穷举≤8/随机/FAS-MaxSINR/
   全 DBF)，平坦 + TDL 各一张；
2. **波束成形增益 vs 损伤 折中图**（min-SINR vs D_max）——招牌图，显示峰；
3. 到全 DBF 差距 vs M；
4. 能效 bits/joule vs 架构（功耗含 RF 链/ADC/开关网络/LO/基带/端口扫描），
   对标 GreenMO/DBF/HBF。

### 7.7 执行顺序与代数回归（沿用 Phase 2 纪律）

- **Part A(~1 周)**：M>N 平台 + A/S/H̃ + 端口位置/孔径/相关（相关系数须绑定
  端口间距与孔径，不用任意指数）+ 硬件约束。回归：M=N 退化逐位一致、理想开关
  退化、占空比守恒、非法并接拒绝；
- **Part B(~1.5 周)**：三算法 + 基线，门 1/2/3 逐一验收；
- **Part C(~0.5 周)**：单链 FAS 定位 + 能效模型；
- **Part D(并行)**：二值 S 闭式 SINR/可达速率；可选同步 CRLB（不阻塞主链）。

每步先写代数回归再跑扫描；每条声称须可从提交版代码 + 记录 seed 逐位复现。
下轮审议编号从 **R16** 起，对象为 Part A 平台 + 退化回归。
