# OTA 验证战役预注册规范（R24–R25）

冻结日期：2026-07-16（首次正式采集前本文件即冻结）
角色：lianpei 执行采集与分析脚本；专家审稿人从原始采集复现并评级，
裁决追加至 REVIEW_VERDICTS.md（实验 A = R24，实验 B/C = R25）。

**冻结规则**（继承 R15 文化）：
1. 结果门禁（G-系列）在各实验首个正式采集被接受后不得修改、不得换口径改判；
2. 有效性门禁（V-系列）失败允许修复后重采，但每次重采必须在本文件末尾
   "偏离记录"追加一行（时间戳+原因）；
3. 预跑（P-系列 pre-flight）允许调整参数，参数在预跑结束后冻结；
4. 负结果如实进论文（先例：R15 EVM² 2.112 no-go 原样报告）。

---

## 0. 硬件与公共事实（依据仓库代码，执行前请逐条确认）

| 项 | 值 | 来源 |
|---|---|---|
| 载频 | 3.2 GHz → **λ = 93.7 mm** | type1_config.m `centerFrequencyHz` |
| 波长间距 | 0.5λ = **46.8 mm**，0.25λ = **23.4 mm**，0.125λ = **11.7 mm** | 换算 |
| TX | 10.156.64.30，4 通道全开（`txChannelMask=f`），衰减 `txAttenuationMdB`（mdB，默认 0=满功率） | type1_config.m / type1_tx.m |
| RX | 10.156.64.41（经 .30 跳板），**4 条相干物理通道**，各 122.88 MS/s，`rxGain=30`（范围 0–73，无 AGC） | type1_digital_switch.m / capture 脚本 |
| 采集单元 | raw122 [N×4]，20 ms/段 ≈ 78.6 MB（complex single） | type1_capture_ota_valid_raw122.m |
| 天线 | 收、发各 4 根外接胶棒天线，位置可任意摆放（若实际数量不符，先在偏离记录标注） | 用户告知 |
| 工作目录 | /home/bupt/tools/matlab_test/nr4x4_type1，数据在 data/ | RUN_COMMANDS.md |

胶棒天线多为 2.4/5 GHz 宽带鞭状，3.2 GHz 下效率非标称——不影响本战役：
所有结论都是同一套天线的**相对**比较。

## 0.1 公共纪律（两个实验通用）

- **静态环境**：采集时人离开房间、关门；所有触发都走 SSH 远程；房间内
  物品在一轮（round）内不得移动（摆天线除外）。
- **增益冻结**：全战役 `rxGain=30`（若预跑发现削波，允许在 P2 一次性下调并冻结）。
- **TX 热稳定**：TX 启动后等待 ≥120 s 才开始正式采集（R15 教训：启动期 IQ 不可作锚点）。
- **噪声地板标定**：战役开始与结束各一次 TX 关断状态下 20 ms 采集
  （新脚本 `type1_capture_noise_floor`），得每通道噪声方差 σ²_c(c=1..4)，
  用于 SNR 归一与 SINR 白化。
- **元数据**：每段采集的 meta 必须含：fileTag、rxGain、TX 衰减、天线位置(mm)、
  通道-位置映射、摆位标签；每个摆位拍一张照片存 docs/ota_photos/。
- **可复现**：所有分析脚本固定随机种子（A:24001，B:25001，C:25002）、
  各跑两次独立 MATLAB 进程做 isequaln 逐位比对；数据目录生成 SHA256SUMS.txt。
- **主张边界**：开关本体始终是数字模拟（作用在真实前端信号上）；独立多用户、
  移动场景仍不可及，论文 §IX 如实划界。

## 0.2 预跑（P-系列，结果不进论文）

- **P0 磁盘**：RX 服务器 `df -h` 确认 data/ 所在盘 ≥ 40 GB 空闲。
- **P1 衰减阶梯探测**（实验 A 用）：每档 TX 衰减各采 1 段，读实测 SNR，
  确认阶梯落点覆盖目标窗（见 §1.2）；允许平移阶梯一次，随后冻结。
- **P2 削波检查**（实验 B/C 用）：满功率、最近摆位下采 1 段，
  检查 max|IQ| < 0.7 满量程；超限则一次性下调 rxGain 并冻结。
- **P3 干跑**：每个新分析脚本先在 R15 旧数据或仿真数据上干跑通过。

---

# 实验 A（R24）：多 SNR 配对注入（TX 衰减遍历）

## 1.0 目的与主张

把 R15 的"33–43 dB 单簇 + 模型偏乐观 2×（单点）"升级为
"**约 25 dB SNR 范围内的模型偏差特性曲线 b(SNR)**"。
论文可主张：多 SNR 趋势验证 + 偏差量化；不可主张：定量标定通过。

## 1.1 代码前置工作（用户实现，共 3 项）

1. **TX 衰减环境变量**：在 type1_config.m 仿照 `TYPE1_CHANNEL_CODING`
   的模式（第 58–64 行）加 `TYPE1_TX_ATTEN_MDB` 覆盖 `cfg.txAttenuationMdB`；
   assert：0–89750 且为 250 的倍数。
2. **采集 campaign 脚本** `type1_run_ota_a_capture_tier.m`：
   输入（tierLabel, maxEvmPct, nSegments=12），循环调用
   type1_capture_ota_valid_raw122（fileTag=`otaA_<tier>_seg<k>`，
   传 tier 专属 `TYPE1_OTA_CAPTURE_MAX_EVM_PCT`），把每段的接受/尝试次数、
   实测 SNR 写入该档 manifest。
3. **分析脚本** `type1_run_ota_a_cross_validation.m`：
   扩展 R15 同段配对逻辑到多档——每段：有开关/无开关 EVM² 罚 +
   10 个 matched-SNR TDL seed 的模型预测罚；输出见 §1.4。

## 1.2 采集协议

- **衰减阶梯**（P1 后冻结）：TYPE1_TX_ATTEN_MDB ∈
  {0, 6000, 12000, 18000, 24000}，名义目标 SNR ≈ 35/29/23/17/11 dB。
- **每档**：TX 端以该衰减启动（`TYPE1_TX_DURATION_SEC=900`），
  等热稳定后 RX 端采 **12 段**、每段 20 ms；换档 = 停 TX
  （`type1_tx_stop`，确认 ret=0）→ 改环境变量重启。
- **每档 EVM 接受门**（随 SNR 放宽）：MAX_EVM_PCT = {20, 20, 30, 45, 60}；
  PSS/PBCH 门全档强制；被拒的 attempt 计入获取统计（不丢弃计数）。
- 命令模板（TX 侧，每档改 ATTEN 值）：

```bash
ssh -tt bupt@10.156.64.30
cd /home/bupt/tools/matlab_test/nr4x4_type1
sudo env TYPE1_TX_DURATION_SEC=900 TYPE1_TX_ATTEN_MDB=12000 \
  /home/bupt/tools/matlab/bin/matlab -batch "type1_tx"
```

- 命令模板（RX 侧，每档一条）：

```bash
ssh -tt -o 'ProxyCommand=ssh -W %h:%p bupt@10.156.64.30' bupt@10.156.64.41
cd /home/bupt/tools/matlab_test/nr4x4_type1
sudo env TYPE1_OTA_CAPTURE_SETTLE_SEC=3 TYPE1_OTA_CAPTURE_BLOCKS=20 \
  /home/bupt/tools/matlab/bin/matlab -batch \
  "type1_run_ota_a_capture_tier('att12000', 30, 12)"
```

## 1.3 有效性门禁（V-系列）

- **V-A1**：≥4 个档的实测中位 SNR 落进 [8, 40] dB 且相邻档间隔 ≥4 dB；
  不满足 → 平移阶梯重采（记偏离）。
- **V-A2**：每档接受段数 ≥10/12；不足 → 该档补采。

## 1.4 预注册端点与结果门禁（G-系列，冻结）

- **主端点 E-A1（表征，无通过/失败）**：每档
  b(档) = median_seg[ OTA 开关罚(EVM²) / 模型预测罚(EVM²) ]，
  bootstrap 2000 次（rng(24001)）95% CI；输出 b(SNR) 曲线。
  R15 单点 2.112 应落在最高档 CI 内（不作门，作一致性陈述）。
- **G-A1（方向一致性）**：逐段"有开关 EVM² > 无开关 EVM²"，
  全部 ~60 段做精确二项符号检验，单侧 p < 0.001 通过。
- **G-A2（单调性 sanity）**：档间中位绝对 EVM² 随衰减单调上升
  （Spearman ρ=1 于 5 档）。
- **次端点 E-A2**：每档获取成功率（接受段/总 attempt）+ Wilson 95% CI，
  与仿真 waterfall 定性叠图（§VI 素材）。
- 两次运行逐位一致后冻结 summary MAT。

工作量：含预跑约半天。产出目录 `data/type1_r24_ota_a_<stamp>/`。

---

# 实验 B/C（R25）：实测信道库战役（天线网格搬移）

## 2.0 目的与三条分析轨

同一批采集，三条预注册分析轨：
- **轨 B（FAS/E2 实测重放）**：单用户（TX 口 1 列）逐端口 SNR 边缘统计
  → 实测 best-of-M 选择增益、实测空间相关 vs J0；
- **轨 C（选择重放）**：实测 Ĥ∈C^{16×4} 信道库上重放"仅选择型"端口调度的
  min-user LMMSE SINR（复用 type1_phase3_theory_metrics 机器）；
- **轨 C2（波形级选择，消费实验 A 的低 SNR 采集）**：会话内 M=4 选 2 重拼接
  → 端到端解码 EVM² 配对比较。

## 2.1 相位不变性引理（轨 C 的合法性依据，写进论文附录）

**引理**：设逐子载波信道 H[k]∈C^{M×N}，仅选择型调度（每条虚拟链在全部
码相只读一个端口，无跨端口聚合）给出 G[k]=SᵀH[k]，S 每列为单位向量。
若各行经历任意未知的逐行相位/定时旋转 H'[k]=D_k H[k]
（D_k 对角、酉），且噪声按每通道实测地板白化，则每用户 LMMSE SINR 不变：
G'ᴴR_n⁻¹G' = GᴴR_n⁻¹G（D_k 与对角阵对易）。

**推论**：跨采集会话拼接的行（不同会话相位/定时参考不同）对仅选择型
调度的 SINR 重放完全合法。**失效范围**：跨端口聚合（fan-in）调度的行间
相位有物理意义，跨会话拼接不合法——轨 C 只重放仅选择型子族
（恰好是 R21 归因指认增益所在的子族；F2 码叠加本就是 null 结果）。
**遗留混杂**：互耦——同时在位天线只有 4/16 根，与真实 16 阵元互耦不同，
论文作 limitation 如实写。

## 2.2 天线排布细则（物理执行手册）

### 2.2.1 RX 网格模板

- 取 ≥75 cm 硬纸板/泡沫板，画一条直线，按下表打刻度并标 P1–P16，
  容差 **±2 mm**（0.125λ 网格 ±1 mm）；模板用胶带平贴桌面。
- **主网格 0.5λ（间距 46.8 mm）刻度 (mm)**：
  0.0, 46.8, 93.7, 140.5, 187.4, 234.2, 281.1, 327.9,
  374.7, 421.6, 468.4, 515.3, 562.1, 609.0, 655.8, 702.6
- 辅网格按 P_k=(k−1)×间距 自算：0.25λ 间距 23.4 mm（跨 351 mm）、
  0.125λ 间距 11.7 mm（跨 176 mm）。

### 2.2.2 天线贴放规则（每一条都影响数据有效性）

1. 四根 RX 胶棒天线**全部竖直**（与 TX 同极化），天线轴心对准刻度点；
   用小泡沫块/纸筒固定馈点直立，馈点离桌面高度四根一致（±5 mm）。
2. **走线**：馈线离开天线后垂直于阵列轴向平铺引出 ≥20 cm 再走弯，
   不得在天线附近盘圈；每次搬位保持走线姿态一致。
3. **净空**：任何天线距金属（YunSDR 机箱、显示器、桌腿）≥30 cm（≈3λ），
   距桌沿 ≥10 cm。
4. **通道-位置映射**：会话 s 时，RX 通道 c 的天线放在 P_{s+4(c−1)}，即

| 会话 | RX1 | RX2 | RX3 | RX4 | 同时在位最小间距 |
|---|---|---|---|---|---|
| 1 | P1 | P5 | P9 | P13 | 4×间距 |
| 2 | P2 | P6 | P10 | P14 | （0.5λ 网格=187 mm） |
| 3 | P3 | P7 | P11 | P15 | |
| 4 | P4 | P8 | P12 | P16 | |

   每会话只需把四根天线各**前移一个刻度**。

### 2.2.3 TX 布置（全战役固定）

- 4 根 TX 胶棒天线竖直、相互间距 ≥30 cm（可摆成矩形四角/架子不同层），
  胶带固定 + 拍照，**整个战役不再移动**。
- **NLOS**：用金属柜/大显示器挡住 TX 与 RX 网格间直射路径，
  或 TX 放拐角/隔断后；TX–RX 距离目标 3–6 m。
- B/C 全程满功率（TYPE1_TX_ATTEN_MDB=0）：高 SNR 对信道估计有利。

### 2.2.4 摆位轮次（rounds，制造信道系综）

- **12 个摆位 L01–L12**：把整块 RX 模板搬到房间内 12 个不同位置/朝向
  （不同桌面、不同高度、朝向转 0°/45°/90°、有的靠墙有的居中），
  每处贴牢、拍照、记摆位标签。
- 每个摆位跑 4 个会话（=16 位置）；**摆位 L01、L07 额外跑辅网格
  （0.25λ 与 0.125λ 各 4 会话）+ 两个稳健性会话**（见 2.4）。

## 2.3 采集协议

- TX 侧每摆位启动一次：`TYPE1_TX_DURATION_SEC=2400`。
- RX 侧每会话：新脚本 `type1_run_ota_bc_session(placement, grid, session)`
  连续接受 **4 段** 20 ms 采集（fileTag=`otaBC_L03_g050_s2_k1` 式），
  meta 写入位置 mm 数组与通道映射；四段间不动天线。
- 会话节奏：进房移天线（约 2 min）→ 出房关门 → SSH 触发采集（约 1 min）。
  单摆位 ≈ 20 min；12 摆位 + 辅网格 ≈ **5–6 小时**（可分两天，
  跨天必须重做噪声标定并记偏离）。
- 存储：(12×4 + 2×8 + 4)×4 段 ≈ 272 段 ≈ 21 GB（P0 已核）。

## 2.4 稳健性检查与有效性门禁

- **V-B1 漂移检查**（L01、L07）：会话 4 结束后按会话 1 摆位重采一次；
  门：逐位置信道频响相关 ≥0.95 且 |ΔSNR| 中位 ≤1 dB；失败 → 该摆位作废重采。
- **V-B2 通道置换检查**（L01、L07）：会话 1 位置不变、天线在 RX1–4 间
  循环换接一位重采；门：逐位置 SNR 差中位 ≤1.5 dB；失败 → 启用每通道
  SNR 标定偏置（由噪声地板+置换数据估计），并在论文如实报告。
- **V-B3**：每会话 4 段全部过 PSS/PBCH/EVM(20%) 门；不足 → 会话内补采。

## 2.5 预注册端点与结果门禁（冻结）

**轨 B（rng 25001）**：
- **E-B1**：每摆位 16 端口 SNR 的四分位距（IQR），12 摆位中位数
  ——实测空间分集"存在量"。
- **E-B2**：best-of-M 增益（M∈{1,2,4,8,16}，嵌套前 M 位置，
  基准=P1 端口）：跨 12 摆位的中位数与全距；与 E2 仿真曲线
  （10.6/12.0/12.6 dB@10% 中断）**并排报告，不设相等门**
  （实验室 K 因子高，预期实测低于 Rayleigh 预测；K 因子随结果报告）。
- **G-B1（可证伪）**：实测空间相关（系综=摆位×抽取子载波）满足
  ρ̂(0.125λ) > ρ̂(0.5λ)；违背 → 如实报告并分析（不改判）。

**轨 C（rng 25002）**：
- 每摆位建 Ĥ[k]∈C^{16×4}（四会话按行堆叠，每通道噪声白化），
  穷举 C(16,4)=1820 个仅选择型子集的 min-user LMMSE SINR。
- **G-C1**：跨 12 摆位，[最优 4/16 子集 − 默认 {P1–P4}] 的符号检验
  单侧 p<0.05（12 摆位需 ≥10 正）通过 → 论文可写
  "实测信道上选择机制成立"。
- **G-C2（单调）**：逐摆位 gain(M=16) ≥ gain(M=8)，符号检验 p<0.05。
- **E-C1**：M∈{4,8,12,16} 实测增益曲线与 TDL 仿真曲线并排（无相等门）。

**轨 C2（消费实验 A 最低两档共 24 段，rng 25002）**：
- 每段前半 blocks 估计逐端口质量选"最优 2/4"，后半 blocks 解码
  （样本外，沿用偶/奇交叉拟合纪律）；重拼接 stitched(t)=raw122(t,σ(t mod 4))
  走既有解码链。
- **G-C2W**：配对 ΔEVM²(最优2子集 − 默认{RX1,RX2}) < 0 的符号检验
  单侧 p<0.05 → 论文可写"波形级 OTA 选择增益（M=4 规模）"。

## 2.6 分析脚本（用户实现，3 个）

1. `type1_run_ota_b_fas_replay.m`（轨 B：manifest → E-B1/2、G-B1、K 因子、ρ̂(d)）
2. `type1_run_ota_c_selection_replay.m`（轨 C：Ĥ 库 → 1820 穷举 → G-C1/2、E-C1）
3. `type1_run_ota_c2_waveform_replay.m`（轨 C2：实验 A raw → 重拼接解码 → G-C2W）

均输出单一 summary struct + 两次运行 isequaln 比对 + SHA-256。

---

## 3. 论文落点映射

| 结果 | 论文位置 |
|---|---|
| b(SNR) 偏差曲线 + G-A1 | §IX-C 升级（"多 SNR 趋势验证"），Fig.13 区 |
| E-A2 获取率 vs 仿真 waterfall | §VI 一句话 + 叠图 |
| 轨 B 实测 FAS 曲线 + K 因子 | Fig.11(a) 叠加实测点，§VII-A |
| G-B1 ρ̂ vs J0 | §V-D J0 讨论一句话 |
| 轨 C G-C1/C2 + E-C1 | §V 新增 "measured-channel replay" 小节 + 引理进附录 |
| 轨 C2 波形级选择 | §V 或 §IX 一段（"M=4 规模端到端 OTA 选择"） |
| 全部 limitation（互耦/静态/相干 TX/数字开关） | §IX 划界段 |

## 4. 审查流程

- R24：实验 A 完成后提交 manifest 路径 → 专家从 raw122 独立复现
  b(SNR)/G-A1/G-A2 → 裁决追加 REVIEW_VERDICTS.md → 通过后才开工 B/C
 （教训前馈）。
- R25：B/C 战役完成后同流程；轨 C2 依赖 R24 原始数据，归入 R25 裁决。

## 5. 偏离记录（执行期间追加，每行：日期 | 条款 | 内容 | 处置）

（空）
